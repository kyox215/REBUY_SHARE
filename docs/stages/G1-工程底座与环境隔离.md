# G1 阶段合同：工程底座与环境隔离

文档状态：G1.2a 本地 workflow/等价验证已完成；G1.2b 真实 PR/CI 已完成并已通过 PR #1 merge commit 合并 main；G1.3-0 本地环境隔离/回退预检与 G1.3-1 provider/CLI/部署前隔离预检均已完成；G1.3-2 高风险独立审查已完成（原方案 NO-GO，修正后为条件 GO 候选），Owner 于 2026-08-27 10:33:59 CEST 批准修正版 G1.3 受控范围，G1.3 bad503 Preview 已真实 READY/health 脱敏通过，good200 本地验证已通过，good200 Actions/Preview 后续未执行；G1 Exit preflight 已完成但结论为 NO-GO；G1.1 Node 22 可复现 frozen install、typecheck、lint、build 已完成，G1 Exit 未通过
阶段 ID：G1  
阶段名称：工程底座与环境隔离  
当前状态：执行中（G1.1 已完成，G1.2a 已完成，G1.2b 真实 PR/CI 已完成并合并 main，G1.3-0 与 G1.3-1 预检已完成，G1.3-2 高风险独立审查已完成，修正版 Owner Gate 已批准，G1.3 bad503 Preview 已真实 READY/health 脱敏通过，good200 本地验证已通过，good200 Actions/Preview 后续未执行）
当前证据级别：本地静态 + 本地等价 + archive 预检 + provider 远端只读 + 高风险独立审查 + 远端只读 + 远端 Actions + main merge closeout
记录日期：2026-08-27（Europe/Rome）
记录性质：G1.1 本地 Git/工具链执行记录、G1.2 最小 CI 预检与 G1.2a 本地 workflow/等价验证、G1.2b 真实 PR/Actions 与远端只读设置核验、G1.3-0 本地环境隔离/回退预检、G1.3-1 provider/CLI/部署前隔离预检、G1.3-2 高风险 Preview 执行独立审查、G1.3 修正版 Owner Gate 授权记录、G1.3-3 good200 本地候选与 bad503 Preview 结果记录、G1 Exit requirement-to-evidence 预检记录；不代表 good200 Actions/Preview、在线回退、生产验收或环境连接。
commit ref：good200 `N/A`（本地候选尚未提交；base=`a228545ce0f36d8206d048a8180897a041d23de0`）；历史 G1.2b main merge `cba97eb4e7c93e5c42ba496e4d2ddeac5b476c7c`（G1.2a workflow 修正 `b0681d585cabe2f5f293779fc3627e2782be9fa2`、初始基线与初版 `a388348b` 均保留为历史；稳定 tag 见 G1.1 证据）
deploy/environment ref：N/A  

## 1. 元数据

- Owner：Rebuy Owner；2026-08-25 23:06:57 CEST 原话 `分类目录IA通过，G0重新冻结，未覆盖项进入后续专项，恢复G1授权`
- 执行代理/负责人：G1 Entry 准备记录由本地文档执行代理整理；工程批次负责人待 Owner 确认
- 审查者：独立 merge reviewer 已对 PR #1 给出 GO；main merge closeout 由主代理负责后续复核
- 起始时间：2026-08-25 23:06:57 CEST（Entry 基线只读准备）；工程实施起始时间：N/A
- 结束时间：N/A
- 记录版本：v8（2026-08-27，G1.3-3 good200 本地候选与 bad503 结果收口）
- 当前只读环境：项目根目录 `/Users/kyox215/Documents/codex应用文件夹/rebuy购物交易计划`；Prototype 位于 `prototype/`
- 本合同不读取任何 `.env` 值，不记录密码、token、PII、真实商家资料或生产凭据。

## 2. 目标、范围与排除

### 目标

在 G0/P1 Owner 明确通过并打开 G1 后，建立可追溯、可回退的工程底座：

1. 确认 Git 根目录、版本基线和可回退引用。
2. 固定 Node 22 与 pnpm `10.33.3`，让本地、CI 和预览使用同一工具链约束。
3. 建立只覆盖 `typecheck`、`lint`、`build` 的最小 CI 门，不把 CI 通过写成业务或生产通过。
4. 明确 Local、Preview、Staging、Production 的项目、数据、环境变量、权限和部署边界。
5. 建立 secret/环境变量清单、预览部署门、版本回退和最小恢复演练。

### 范围

- 根目录 Git 选择、初始版本标签/提交基线和回退引用的 Owner 决策与记录。
- `prototype/` 的 Node 22、pnpm `10.33.3`、现有 `pnpm-lock.yaml` 和脚本约束核对。
- CI 所需的 Node/pnpm 固定、依赖安装策略、`pnpm typecheck`、`pnpm lint`、`pnpm build` 和产物/日志边界。
- 四环境隔离合同、Preview 入口与回退门；只建立可审查的工程边界，不顺手实现业务能力。
- 变量名级别的环境清单和 secret 归属；不复制或读取任何变量值。

### 排除与停止边界

- 除 G1.3-3 允许新增的 `prototype/app/api/health/app/route.ts` 外，本阶段不修改 prototype 源码/配置、package/lockfile；G1.2a 仅新增本地只读 workflow，G1.2b 仅按一次性 Owner 授权执行 canonical repo 的 integration/PR/Actions，Git 初始化仅限项目根本地基线，依赖安装仅在隔离副本验证。
- 不创建数据库、Supabase 项目、Storage、RLS、Auth、OAuth、SMTP、支付、物流、生产部署或真实身份。
- 不接 production，不迁移真实数据，不读取 `.env`、个人配置、密码、token、客户 PII、商家资料或生产日志。
- 不把 Node/pnpm 版本检查、lockfile 存在或本地 HTTP 证据解释为 G1 已完成。
- Merchant Shell、商家后台、账号权限、数据库 schema 和业务 API 仍受后续阶段合同约束。

## 3. 依赖与前置条件

| 依赖/前置 | 所需证据 | 当前结果 | 记录链接 |
|---|---|---|---|
| GOV-1 治理 Gate | Owner 已明确决定并在 15/阶段记录可追溯 | 已通过；不等于 G1 已打开 | [GOV-1 记录](./GOV-1-治理文档与状态台账.md) |
| G0/P1 视觉 Gate | Owner 全流程视觉验收，并维持当前“通过并冻结” | 2026-08-25 23:06:57 CEST Owner 原话已通过并冻结；G1 Entry 恢复，G1.1 已进入执行但不等于 G1 工程完成 | [G0 记录](./G0-P1-视觉验收与UI冻结.md)、[Owner 清单](./G0-P1-Owner视觉验收清单.md) |
| Git 根与版本基线 | 可用 `.git`、根目录选择、初始 ref 和回退点 | 已满足本地 root/main/identity/初始 SHA；G1.2b 已在 canonical public repo 建立受限 integration/PR/Actions 证据，并以 merge commit 合并至远端 `main`；integration 分支仍保留 | [G1.1 基线证据](../evidence/G1/2026-08-25-g1-1-local-baseline/README.md)；[G1.2b 远端证据](../evidence/G1/2026-08-27-g1-2b-remote-ci/README.md)；[G1.2b main merge closeout](../evidence/G1/2026-08-27-g1-2b-main-merge/README.md) |
| Node/Corepack/pnpm 工具链 | Node 22、Corepack `0.34.6`、pnpm `10.33.3`、lockfile 和可复现安装策略 | fresh exact bootstrap 下 Node `v22.12.0`、Corepack `0.34.6`、pnpm `10.33.3`；frozen install、typecheck、lint、build 均通过；安装 warning 仅为 pnpm 忽略 `unrs-resolver@1.12.2` build scripts | [G1.2a 接续证据](../evidence/G1/2026-08-26-g1-2a-local-workflow/README.md) |
| 环境/secret 所有者 | Local/Preview/Staging/Production 责任人、资源和变量边界 | G1.3-0 本地合同/忽略规则/版本取回预检与 G1.3-1 provider/CLI 预检已完成；Vercel team/project/旧 READY production asset 已认证只读发现但未由当前 repo 接受或修改；bad503 Preview 已有脱敏 READY/health 结果，good200 本地验证已通过且未部署；Supabase 无 Rebuy 项目；当前 repo 已有 bad503 Preview；good Preview/Staging 尚无，Production 未获验收；变量仍只记录名称/来源 | [G1.3-1 provider/Preview 预检证据](../evidence/G1/2026-08-27-g1-3-1-provider-preview-preflight/README.md)；[G1.3-3 good200 本地候选 evidence](../evidence/G1/2026-08-27-g1-3-3-good200-local-candidate/README.md)；[G1.3-0 预检证据](../evidence/G1/2026-08-26-g1-3-0-local-environment-preflight/README.md)；本记录第 7–9 节 |

## 4. 已核实的只读现况

以下事实来自本批只读检查与 G1.1 执行命令；除 Git 本地基线与文档证据外，没有读取 `.env` 值或连接外部服务：

| 检查 | 实际结果 | 解释边界 |
|---|---|---|
| 项目根 Git 元数据 | 根目录 `.git` 已建立；本地 `main`；初始 ref `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb` | G1.1 Git 基线已建立；G1.2b 另有 canonical public repo 的受限 integration/PR/Actions；PR #1 已通过 merge commit 合并 `main`，不代表 Preview/部署存在 |
| `node --version` | G1.1 明确 PATH 下为 `v22.12.0` | 满足版本目标；隔离 frozen install 与脚本复验通过 |
| `pnpm --version` | `10.33.3` | 与项目声明一致；隔离 frozen install 与脚本复验通过 |
| `prototype/.node-version` | 文件存在，内容为 `22` | 是版本意图，不是当前运行时证明 |
| `prototype/.nvmrc` | 不存在 | 不新增；由 G1 Owner 决定是否只保留 `.node-version` 或增加统一约束 |
| `prototype/package.json` engines | `node: 22.x`、`pnpm: 10.33.3` | 项目运行时合同；隔离 Node22 frozen install 已完成 |
| `prototype/package.json` packageManager | `pnpm@10.33.3` | 目标包管理器版本 |
| `prototype/pnpm-lock.yaml` | 存在，`lockfileVersion: '9.0'` | 可作为安装基线输入；不等于 lockfile 已在 Node 22/CI 复验 |
| Prototype scripts | `dev: next dev`、`build: next build`、`start: next start`、`lint: eslint .`、`typecheck: tsc --noEmit` | 隔离 Node22 frozen install 后 typecheck/lint/build 均退出 0 |
| 关键依赖 | `next`、`react`、`react-dom`、`lucide-react`、`@supabase/ssr`、`@supabase/supabase-js`；开发依赖含 TypeScript、ESLint 和 Next ESLint 配置 | 仅为依赖清单事实；不代表 Supabase/Auth 已连接或实施 |
| `prototype/.env.example` | 只观察到变量名 `NEXT_PUBLIC_SUPABASE_URL`、`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`；注释限定 A1 独立测试环境 | 未读取赋值；不代表远端项目、Auth、Staging 或生产存在 |

## 5. 决策与假设变化

| 日期（Europe/Rome） | 决策/假设 | 来源或 Owner 决定 | 对范围/阶段的影响 |
|---|---|---|---|
| 2026-08-25 | G1 必须位于 G0/P1 之后、G2-A0/G2-A1 之前 | 14 全局路线与 Owner 已批准治理路线 | G0 未通过前只准备合同，不打开 G1 |
| 2026-08-25（Entry 恢复前快照） | Node 目标为 22，pnpm 固定为 `10.33.3` | `prototype/.node-version`、`package.json` engines/packageManager | 当时 Node `20.20.2` 是待解决差距；不得以 warning 通过替代对齐 |
| 2026-08-25（Entry 恢复前快照） | 根目录 Git 基线必须如实建立，不能捏造 ref | 只读检查：当时根目录无 `.git` entry | 后续 G1.1 按 Owner 范围采用项目根 Git 根并记录初始 ref；不添加 remote |
| 2026-08-25 | 四环境隔离和变量边界先于任何远端/Auth/生产连接 | 14 G1 合同与本合同 | G1 不创建数据库、不执行 Auth、不接 production |
| 2026-08-25 | Owner 原话 `继续下一步`；项目根为唯一 Git 根、`prototype/` 为 app 目录 | 本轮建立 `main`、repo-local identity 和初始 SHA；Node `v22.12.0`/pnpm `10.33.3` 已确认，但 offline frozen install 因缺少 `@swc/helpers@0.5.23` 阻塞 | G1.1 部分完成，G1.2/G1.3 未开始 |
| 2026-08-26（历史快照） | G1.1 隔离 Node22/pnpm frozen install、typecheck、lint、build 已完成；G1.2 拆分为本地 workflow/等价验证（G1.2a）与未来远端真实 run（G1.2b） | 当时的 G1.2 预检与 G1.2a 证据记录了 workflow 修正 commit `b0681d58`、初版 `a388348b` 和当时无 remote 的状态 | 当时 G1.2a 已完成本地静态/fresh 等价证据，G1.2b 待 Owner Gate；当前 G1.2b 事实以本合同 2026-08-27 追加记录和 15 台账为准 |

历史事实和 Owner 决定只能追加；发现环境事实变化时新增日期条目，不静默改写本节。

## 6. 执行批次

| 批次 | 目标 | 实际结果 | 状态 |
|---|---|---|---|
| G1.0 预编制合同 | 记录真实工具链、Git 缺口、四环境、CI、secret 和回退要求 | 本文件已创建；G1.1 已启动 | 已完成预编制 |
| G1.1 版本与 Git 基线 | 建立可追溯 ref、固定 Node 22/pnpm 并验证 lockfile | Git/main/identity/初始 SHA 与版本已建立；隔离 Node22/pnpm `10.33.3` frozen install、typecheck、lint、build 均退出 0；保留 `unrs-resolver@1.12.2` ignored build-script warning | 已完成 |
| G1.2 最小 CI | 拆分 G1.2a 本地 workflow/等价验证与 G1.2b 远端真实 run，使用 Node 22/Corepack `0.34.6`/pnpm `10.33.3` 运行 frozen install/typecheck/lint/build | [G1.2a 接续证据](../evidence/G1/2026-08-26-g1-2a-local-workflow/README.md)；[G1.2b 远端 PR/CI 证据](../evidence/G1/2026-08-27-g1-2b-remote-ci/README.md)；[G1.2b main merge closeout](../evidence/G1/2026-08-27-g1-2b-main-merge/README.md)；[G1.2b-0 远端目标审计](../evidence/G1/2026-08-26-g1-2b-0-remote-target-audit/README.md)；[G1.2b-1 历史保全与敏感审计演练](../evidence/G1/2026-08-26-g1-2b-1-local-integration-rehearsal/README.md)；workflow 修正 commit `b0681d585cabe2f5f293779fc3627e2782be9fa2`；本地 fresh 等价、PR run 和 main push run 均成功 | G1.2a、G1.2b 已完成，PR #1 已以 merge commit 合并 `main`；不等于 G1 Exit 通过 |
| G1.3 四环境与预览回退 | 建立 Local/Preview/Staging/Production 隔离记录和预览/回退演练 | G1.3-0 已完成本地环境隔离、忽略规则、版本取回与回退手册预检；G1.3-1 已完成 provider/CLI/部署前隔离预检；G1.3-2 已完成高风险独立审查（原方案 NO-GO，修正后条件 GO 候选）；Owner Gate 已批准受限 implementation；bad503 Preview 已真实 READY，deployment resolved config `builds[0].config.nodeVersion=22.x`、root HTTP 200、health HTTP 503/status unhealthy/no-store、Protection 与 Production 不变量已通过；good200 本地候选验证已通过，exact-head Actions/独立 Preview deploy 未执行；当前 repo 已有 bad503 Preview；good Preview/Staging 尚无，Production 未获验收，在线回退未演练；详见 [G1.3-3 evidence](../evidence/G1/2026-08-27-g1-3-3-good200-local-candidate/README.md) | G1.3 已授权/执行中；bad503 半程完成；good200 本地 install/typecheck/lint/build 已通过，exact-head Actions/独立 Preview deploy 未执行，在线 rollback 仍缺 |

### 6.1 复用 / 扩展 / 新建分类

| 分类 | 组件、模块、规则或资产 | 处理说明 | 是否改变业务事实 |
|---|---|---|---|
| 复用 | `prototype/.node-version`、`prototype/package.json` engines/packageManager/scripts、`prototype/pnpm-lock.yaml`、`.env.example` 变量名 | 作为 G1 版本和变量边界的输入；不复制值、不改文件 | 否 |
| 扩展 | 14 的 G1 合同、15 台账、stages 索引和阶段模板 | 将现况和执行矩阵落成独立预编制阶段合同 | 否 |
| 新建 | `.github/workflows/prototype-quality.yml`、`docs/evidence/G1/2026-08-26-g1-2a-local-workflow/README.md` | 按 Owner G1.2a 授权新增一个只读最小 workflow 和本地等价证据；不新增 remote/环境 | 否 |

## 7. 影响清单

| 影响域 | 文件/组件/路由/API/类型/schema/数据 | 变更摘要 | 证据或 N/A |
|---|---|---|---|
| 文件 | 新建本记录与 G1.3-3 evidence；同步 `docs/stages/README.md`、`docs/15-项目状态与阶段台账.md` 的 G1 链接 | 文档导航和阶段合同链接；本批仅新增 health 路由，其他 prototype/package/lockfile/config/UI 不变 | 本地静态 |
| 组件 | 无；不涉及 AppShell、BrandMark、商品卡、MerchantShell | 不修改 UI 或业务组件 | N/A |
| 路由 | `prototype/app/api/health/app/route.ts` | G1.3-3 新增最小本地 health Route Handler；GET 200、JSON 仅 `status=healthy`、`Cache-Control: no-store`；不读 env、不联网、不调用 Supabase/Auth/DB、不写日志 | G1.3-3 本地静态/HTTP 验证 |
| API | 仅本地 `/api/health/app` | 只提供无外部依赖的本地 health 候选；不连接 Supabase/Auth/外部 API | G1.3-3 本地验证 |
| 类型 | 无 | 不改 TypeScript 类型或配置 | N/A |
| schema | 无 | 不创建/修改数据库 schema、RLS、迁移 | N/A |
| 数据 | 无真实数据写入；只记录版本事实和变量名称 | 不读取或保存 `.env` 值、PII、生产数据 | N/A |

## 8. Local / Preview / Staging / Production 隔离合同

| 环境 | 允许用途 | 数据/资源边界 | 必须满足的门 | 当前状态 |
|---|---|---|---|---|
| Local | 开发、静态检查和本地 Prototype 预览 | 合成数据/专用测试值；不得使用 production secret、真实 PII 或生产数据库 | Node 22、Corepack `0.34.6`、pnpm 固定、lockfile 可复现、脚本结果可追溯 | Git/main 已建立；fresh exact bootstrap 后 Node22/Corepack `0.34.6`/pnpm `10.33.3` frozen install、typecheck、lint、build 已完成；G1.2a workflow 已创建并等价验证；G1.3-0 隔离/取回/回退预检与 G1.3-1 provider/CLI/部署前隔离预检均已完成 |
| Preview | 从可追溯版本生成的短期预览和 Owner 复核 | 独立项目/变量/域名；仅合成或明确脱敏数据；禁止 production secret 和写入 | G1 CI 通过、预览部署门、健康检查、回退 ref 和访问边界 | bad503 Preview 已有真实 READY/health 脱敏结果；good200 本地验证已通过，exact-head Actions/独立 deploy 门禁仍未运行；Vercel project 仅作只读盘点，good Preview 尚未创建 |
| Staging | 隔离的预发布测试，供未来 G2/P2 等专项验证 | 独立资源、账号、数据库和变量；不得与 production 共享写入或凭据 | G1 环境边界合同；后续 G2-A1/P2 各自安全、Auth、RLS 和数据门 | 当前 repo 尚无 Staging；G1.3-0 已满足本地隔离合同，G1.3-1 完成 provider 只读预检；实际 Staging 资源、账号、Auth/DB/RLS/PII 实施属于 G2-A1/P2 后续专项，不是 G1 Exit 前置 |
| Production | 受控生产运行 | 生产专用资源、secret、监控、备份、回滚和书面批准；禁止从 Local/Preview 复用变量 | G1 环境边界合同；所有后续 Production Owner Gate、发布候选、回滚/恢复演练 | Production 未获验收；G1.3-0 已满足本地边界合同，G1.3-1 仅完成 provider 只读预检；实际 Production 资源、secret、监控、备份/恢复和 PII 实施属于 P7/Production 后续专项，不是 G1 Exit 前置 |

环境隔离至少要记录：项目/资源归属、部署来源 ref、变量名称与来源、数据库/Storage 连接边界、访问角色、日志/PII 策略、回滚 ref 和 Owner 批准。值、token、真实 URL 和个人凭据不得写入本合同。

Vercel team/project、旧 READY production asset 与 Supabase 组织/项目已完成最小认证只读盘点；Vercel 旧 production asset 尚未由当前 repo 接受或修改，Supabase 无 Rebuy 项目。按 Vercel 官方 Node 规则，`prototype/package.json` 的 `engines.node=22.x` 覆盖 project 当前显示的 Node `24.x`，默认不 PATCH project Node 设置，必须以新 Preview deployment 的 provider-resolved config `builds[0].config.nodeVersion=22.x` 为主证据，build log 仅作补强；resolved 缺失或非 22 立即 STOP。该事实不授权创建、连接、改 Node、改 alias 或推断其他外部资源不存在。

## 9. 版本基线、Git 与工具链合同

### 9.1 Owner 决策点

G1.3 开工前必须由 Owner 明确并记录；G1.2a 已按 Owner 授权完成，G1.2b 已按一次性 Owner 授权完成：

- Git 根目录是当前项目根还是明确的子目录；远端/分支策略和初始基线 ref。本轮按 Owner 范围采用项目根为唯一 Git 根、`prototype/` 为 app 目录；canonical repo 为 `kyox215/REBUY_SHARE`，采用保留双方历史的 `integration/g1-2b` PR 策略；PR #1 已由独立复审 GO 后以 merge commit 合并 `main`，integration 分支仍保留。
- Node 22 的发行/管理方式，以及是否只以 `prototype/.node-version` 作为开发者约束或补充 CI 声明；不因方便而让 Node 20 继续作为通过环境。
- pnpm `10.33.3` 的固定方式、`prototype/pnpm-lock.yaml` 的校验策略和依赖安装是否启用 frozen lockfile。
- G1.2a 的 Owner 授权已记录并执行：接受 GitHub Actions 候选、`ubuntu-24.04`、Node `22.12.0`、npm exact bootstrap Corepack `0.34.6`/pnpm `10.33.3`、`prototype` working-directory、15 分钟 timeout、`pull_request` + `push main`、`contents: read`，首次关闭 cache、无 secrets/health/artifact/deploy。
- G1.2b 的 remote、integration/PR 和真实 GitHub Actions run 已按 Owner 一次性授权执行；该授权不扩展到 Preview、Supabase/Auth/DB、Staging、Production 或读取 secrets。
- Preview 的部署来源、保留版本数量、回退负责人和停止入口；未有决定不得创建远端项目。

本轮按 Owner 范围执行项目根唯一 Git 根、`prototype/` app 目录；根 `.git`、本地 `main`、repo-local identity 和初始 ref 已建立。G1.2b 已按一次性授权在 `kyox215/REBUY_SHARE` 上完成 integration/PR/真实 Actions；PR #1 经独立复审 GO 后以功能合并 merge commit `cba97eb4e7c93e5c42ba496e4d2ddeac5b476c7c` 合并，docs-only closeout PR #2 随后以 merge commit `e0be39caeea27b7daebadf43794ed9222a45c120` 合并至远端 `main`；当前 `HEAD` 必须以 `origin/main` 实时核对，不将功能合并 ref `cba97eb4e7c93e5c42ba496e4d2ddeac5b476c7c` 写作当前 main，`integration/g1-2b` 仍保留；main push run/job `33033439072` / `98390866073` 成功。fresh exact bootstrap 下 Node `v22.12.0`、Corepack `0.34.6`、pnpm `10.33.3` 的 frozen install、typecheck、lint、build 已完成；仍保留 `unrs-resolver@1.12.2` ignored build-script warning，不执行 approve-builds/allowlist。G1.2 证据见[G1.2 预检记录](../evidence/G1/2026-08-26-g1-2-ci-preflight/README.md)、[G1.2a 接续证据](../evidence/G1/2026-08-26-g1-2a-local-workflow/README.md)、[G1.2b 远端证据](../evidence/G1/2026-08-27-g1-2b-remote-ci/README.md)和[G1.2b main merge closeout](../evidence/G1/2026-08-27-g1-2b-main-merge/README.md)；当前 workflow 修正 commit 为 `b0681d58`。

### 9.2 工具链目标

| 项目 | 目标合同 | 当前事实 | 缺口 |
|---|---|---|---|
| Node | `22.x`，与 `.node-version` 和 `package.json.engines` 一致 | 隔离复验实际 `v22.12.0`；远端 Actions 使用同一版本合同；按 Vercel 规则，`engines.node=22.x` 覆盖 project 当前显示的 Node `24.x` | 默认不 PATCH project Node 设置；Preview deployment 的 provider-resolved config `builds[0].config.nodeVersion=22.x` 必须存在且为 22.x；build log 仅作补强；resolved 缺失或非 22 立即 STOP并另开 Owner 决策 |
| Corepack/pnpm | Corepack `0.34.6` + pnpm `10.33.3`，与 `packageManager` 一致 | fresh exact bootstrap 实际 `0.34.6` / `10.33.3`；远端 Actions 已通过同一 bootstrap 顺序 | 当前 repo 已有 bad503 Preview；good Preview/Staging 尚无，Preview runner/部署安装仍需独立验证 |
| Lockfile | `prototype/pnpm-lock.yaml`，lockfileVersion `9.0` | 隔离 Node22 frozen install 通过；远端 Actions 已通过 frozen install | good Preview 仍需独立验证 |
| Git | 可追溯 root、ref、变更和回退 | root/main/初始 SHA 已建立；canonical repo 已有双 parent integration/PR；PR #1 已以 merge commit 写入远端 `main`，integration 分支仍保留 | PR/Actions/main merge 证据已补充；good Preview 与在线回退仍待 G1.3 |

## 10. 最小 CI 与 Preview 回退门

本节是 G1 目标合同和 G1.2/G1.3 当前实现边界；本地 workflow/等价结果不能替代远端证据，G1.2b 远端 PR/Actions 与 main merge 结果见[G1.2b 远端证据](../evidence/G1/2026-08-27-g1-2b-remote-ci/README.md)及[main merge closeout](../evidence/G1/2026-08-27-g1-2b-main-merge/README.md)。候选与 preflight 见[G1.2 预检记录](../evidence/G1/2026-08-26-g1-2-ci-preflight/README.md)，实际 G1.2a 证据见[G1.2a 接续记录](../evidence/G1/2026-08-26-g1-2a-local-workflow/README.md)：

1. CI 使用 Node 22、Corepack `0.34.6` 和 pnpm `10.33.3`，安装策略与 `prototype/pnpm-lock.yaml` 一致。
2. 定向门至少运行 `pnpm typecheck`、`pnpm lint`、`pnpm build`；失败不得生成可晋级 Preview 的成功标记。
3. CI 输出只保留必要构建/检查摘要，禁止打印 env 值、secret、个人路径中的敏感信息或 `.env` 内容。
4. Preview 只能由已通过 CI 的可追溯 ref 生成；记录部署 ref、时间、负责人、健康检查和可回退版本。
5. 回退演练至少验证：停止错误 Preview 入口、切换到上一可验证 ref、恢复访问边界、保留失败日志摘要且不复制凭据。
6. CI/Preview 通过不等于 G1 Owner Gate 通过，也不等于 Staging/Production 通过。

G1.2 拆分为 G1.2a（本地 workflow 候选与本地等价验证）和 G1.2b（canonical remote 上的真实 GitHub Actions run 及经独立 GO 的 PR #1 main merge）；两者均已形成证据，但 G1.2b 仍不证明 G1 Exit。G1.3-0 已完成本地隔离/取回/回退预检，G1.3-1 已完成 provider/CLI/部署前隔离预检，G1.3-2 已完成高风险独立审查（原方案 NO-GO，修正后条件 GO 候选）；Owner Gate 已批准受限 implementation；bad503 Preview 已真实 READY/health 脱敏通过，good200 本地验证已通过，exact-head Actions/独立 Preview deploy 仍未运行，在线回退仍未实施。

### 10.2 G1.3-2 高风险 Preview 执行独立审查（修正后条件 GO，尚未实施）

独立审查针对 Preview 部署、环境变量、Deployment Protection、Production alias 和在线回退，结论为：原执行草案 `NO-GO`；按以下修正后仅为条件 `GO` 候选。本节保留 Owner Gate 批准前的审查合同；现已由本文件 §18 的 Owner Gate 记录批准受限 implementation，仍必须以实时证据确认，不能把本节写成已执行：

1. `prototype/package.json` 的 `engines.node=22.x` 按 Vercel 官方规则覆盖 project 当前显示的 Node `24.x`；默认不 PATCH project Node 设置，Preview deployment 的 provider-resolved config `builds[0].config.nodeVersion=22.x` 必须存在且为 22.x；build log 仅作补强；resolved 缺失或非 22 立即 STOP。
2. 从精确 `main@af6d7419956ce6640c0b4af5df4db0369e793f77` 建立临时 `git archive`，从归档副本的仓库根执行 `vercel link/deploy`；provider `rootDirectory=prototype` 保持不变，禁止从 archive/prototype 执行以免 `prototype/prototype`，不做 project PATCH。
3. Preview 前只核对变量名和 target，不读取值；任一应用变量存在即停止。Deployment Protection 不得关闭，访问使用 `vercel curl`。
4. 旧 production deployment `dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH` 与 aliases 在部署前后必须保持相同 fingerprint；不 promote、不切 alias、不 rollback Production。
5. `bad503` 只在独立 `DO NOT MERGE` Draft PR/演练分支中进行，PR 永不 Ready/merge，关闭 PR 但保留分支；`good200` 及后续永久 good route 另从干净 main/最终集成分支进入独立普通 PR。

完整审查记录见 [G1.3-2 Preview execution review](../evidence/G1/2026-08-27-g1-3-2-preview-execution-review/README.md)。

### 10.1 Action 不可变候选快照（2026-08-26）

以下 pin 由主代理沿官方 release → commit 链路核验，已用于 G1.2a 本地 workflow；不代表 G1.2b Owner Gate 或远端 CI 已通过：

| Action | Release | 完整 commit SHA |
|---|---|---|
| `actions/checkout` | [v7.0.1](https://github.com/actions/checkout/releases/tag/v7.0.1) | [`3d3c42e5aac5ba805825da76410c181273ba90b1`](https://github.com/actions/checkout/commit/3d3c42e5aac5ba805825da76410c181273ba90b1) |
| `actions/setup-node` | [v7.0.0](https://github.com/actions/setup-node/releases/tag/v7.0.0) | [`820762786026740c76f36085b0efc47a31fe5020`](https://github.com/actions/setup-node/commit/820762786026740c76f36085b0efc47a31fe5020) |

建议的 `uses:` 形式（实际文件见 `.github/workflows/prototype-quality.yml`，当前实现 ref 为 `b0681d58`）：

```yaml
- uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
- uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
```

根据 [GitHub secure-use 指南](https://docs.github.com/en/actions/reference/security/secure-use)，完整 commit SHA 是当前唯一不可变引用方式。实施当天必须再次从官方 release 页面进入 commit 页面核验；若 tag、release、commit 链路或官方稳定候选发生漂移，先更新本合同、[G1.2 预检证据](../evidence/G1/2026-08-26-g1-2-ci-preflight/README.md)和 Owner 决策，不静默替换。核验失败时停止晋级；错误 pin 只能删除或回退当前 workflow 修正 commit `b0681d585cabe2f5f293779fc3627e2782be9fa2`，必要时回退初版 `a388348b81300ca00f669d0bd62b0748b9f191a5`，不修改或删除 G1.1 ref/tag。当前 G1.2a 已获批并完成 fresh 本地验证，G1.2b 真实 PR/Actions 已完成；Preview 仍待 G1.3 Owner Gate。

## 11. 环境变量与 secret 边界

本批只记录已观察到的变量名，不记录值：

| 变量名 | 来源/用途 | 允许环境 | 当前状态 |
|---|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | `.env.example` 注释限定的 A1 独立测试环境公开 URL 配置 | 未来独立 local/preview-staging，需 G2-A1 明确批准 | 仅变量名已观察；未读取值，未连接环境 |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | `.env.example` 注释限定的 A1 独立测试环境公开 publishable 配置 | 未来独立 local/preview-staging，需 G2-A1 明确批准 | 仅变量名已观察；未读取值，未连接环境 |

- `NEXT_PUBLIC_` 变量即使属于公开客户端配置，也不能被当作 production 连接授权；本合同不提供值。
- 当前未读取任何 `.env`、`.env.local`、CI secret、个人 shell 配置、生产 URL 或 secret manager 内容。
- Preview 部署前只记录变量名称与 target；任一应用变量在 team/project/Preview target 中存在即停止，不删除、不修改、不读取值或继续部署。
- 不关闭或改弱 Deployment Protection；Preview 页面与无外部依赖路径使用 `vercel curl` 做访问验证，不把保护绕过 secret 写入文档。
- G1 只建立变量名、所有者、来源环境和泄露处理规则；真实 Auth/数据库变量与 service-role 等高风险值由 G2-A1/P2 专项合同决定，不在本阶段创建。
- 若发现 secret 或 PII 进入日志、Preview、CI artifact 或文档，立即停止晋级，撤销暴露入口，追加事件记录并按 Owner 决定恢复。

## 12. 验证证据与矩阵

### 12.1 本批执行与只读检查

| 验证命令或检查 | 环境/视口 | 代码状态或提交 | 原始结果摘要 | 证据级别 |
|---|---|---|---|---|
| `git init -b main` 与根目录检查 | 项目根目录 | 初始 ref `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb` | `.git`、本地 `main` 和 repo-local identity 已建立；G1.2b 另有受限 canonical remote/PR 证据 | 本地静态 + 远端只读 |
| `node --version` | 明确 Node22 PATH | 初始 ref `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb` | `v22.12.0`；与隔离 frozen install、typecheck、lint、build 结果一致 | 本地静态 |
| `pnpm --version` | 明确 Node22 PATH | 初始 ref `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb` | `10.33.3`；与声明一致 | 本地静态 |
| `prototype/.node-version` | 本地文件 | commit ref `N/A` | 存在，内容 `22` | 本地静态 |
| `prototype/package.json` 选定字段 | 本地文件 | commit ref `N/A` | engines、packageManager、scripts 和关键依赖已读取；未修改 | 本地静态 |
| `prototype/pnpm-lock.yaml` 头部 | 本地文件 | commit ref `N/A` | 存在，lockfileVersion `9.0` | 本地静态 |
| `.env.example` 变量名扫描 | `prototype/.env.example` | commit ref `N/A` | 只记录 `NEXT_PUBLIC_SUPABASE_URL`、`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` 和 A1 独立测试用途；值未读取 | 本地静态 |
| `pnpm install --frozen-lockfile` | git archive 隔离副本、Node22/pnpm10.33.3 | 初始 ref `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb` | 退出 0；351 个包完成安装；pnpm 仅 warning 忽略 `unrs-resolver@1.12.2` build scripts | 本地静态 |
| `pnpm typecheck` / `pnpm lint` | 同一隔离副本 | 初始 ref `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb` | 均退出 0 | 本地静态 |
| `pnpm build` | 同一隔离副本、Node22/pnpm10.33.3 | 初始 ref `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb` | Next `16.3.2` 编译、TypeScript、静态页生成和优化均完成，退出 0 | 本地静态 |
| G1.2a workflow 静态与本地等价检查 | 项目根 workflow + fresh prefix/COREPACK_HOME 隔离副本；该项不依赖 remote | 当前 workflow 修正 commit `b0681d585cabe2f5f293779fc3627e2782be9fa2` | YAML/策略断言通过；Node `v22.12.0`、Corepack `0.34.6`、pnpm `10.33.3`；frozen install/typecheck/lint/build 均退出 0；`prototype/**` 无差异 | 本地静态 + 本地等价 |
| G1.2b canonical PR/Actions | `kyox215/REBUY_SHARE`；PR #1；初始 integration head `f746dca...` | 初始 run `33027593355` / job `98372467897`；文档 head `f8f79cf...` 的独立 check `33029316211`；docs head `cce03ac...` 的 run `33029927182` / job `98379847069`；merge head `cba97eb...` 的 main run `33031297793` / job `98384190584` | 初始 head、文档更新 head、merge head 的 install/typecheck/lint/build 均 success；PR #1 已 MERGED，integration 分支仍保留 | 远端只读 + 远端 Actions |

### 12.2 G1 开工后的验证矩阵（规划）

| 验证项 | Local | Preview | Staging | Production | 目标证据 |
|---|---|---|---|---|---|
| Node/Corepack/pnpm/lockfile | 22 / 0.34.6 / 10.33.3 / frozen 安装 | `engines.node=22.x` 覆盖 project Node24；默认不 PATCH，deployment provider-resolved config `builds[0].config.nodeVersion=22.x` 为主证据；build log 仅作补强；resolved 缺失或非 22 立即 STOP；同一版本/安装 | 同一部署构建来源 | 发布候选 ref | 本地静态/独立测试，按环境追加 |
| typecheck/lint/build | 必须通过 | 只接受通过 ref | 复用并核验部署产物 | 发布候选必须通过 | 本地静态/独立测试 |
| 变量名/来源/权限 | 测试值或无连接 | 独立 Preview 变量 | 独立 Staging 变量 | 独立 Production 变量 | 本地静态/独立测试 |
| 部署来源/回退 | 本地 ref | 精确 archive ref + 仓库根执行 CLI；provider `rootDirectory=prototype` 保持不变，禁止从 archive/prototype 执行；bad503 Draft PR → good200 独立普通 PR | 受控 Staging ref | 发布候选 + 书面批准 | 本地交互/独立测试/后续生产门 |
| secret/PII 日志边界 | 无值记录 | 脱敏检查 | 独立审查 | 生产专项审查 | 本地静态/专项审查 |

## 13. 复用或跳过的验证

| 验证类别 | 复用/跳过 | 原因 | 边界与后续补验 |
|---|---|---|---|
| `pnpm install --frozen-lockfile` | 已通过 | fresh exact Corepack `0.34.6` + pnpm `10.33.3` 的 git archive 隔离副本退出 0；351 个包完成安装，复用 351、下载 0 | 不代表 CI/Preview/Staging/Production 安装已验证 |
| `pnpm typecheck` / `pnpm lint` | 已通过 | 同一 fresh exact 隔离副本、安装成功后均退出 0 | 仅证明本地静态工具链，不替代 CI |
| `pnpm build` | 已通过 | 同一隔离副本、Node22/Corepack0.34.6/pnpm10.33.3 下退出 0 | 不代表部署或生产通过 |
| Browser/E2E | 跳过 | 不修改页面、交互或服务器 | G0 Owner 验收和后续阶段按各自合同执行 |
| Git 初始化/提交 | 已完成本地基线；G1.2b 受限远端流程已执行并完成 main merge | 项目根 `git init -b main`、repo-local identity、初始提交；canonical PR #1 已以 merge commit `cba97eb...` 写入远端 `main`，integration 分支仍保留 | 后续文档可在 integration 分支以普通提交回退；main 只能通过独立 GO 后的非强制 PR merge 维护；禁止直接 push main、force-push、删除远端历史或改写历史 |
| Preview/Staging/Production | Preview 仅按已批准门禁；Staging/Production 仍禁止 | 当前 repo 已有 bad503 Preview（按门禁完成）；good Preview/Staging 尚无，Production 未获验收；Vercel 旧 production asset 仅已认证只读发现且未由当前 repo 接受或修改；G1.3-2 要求变量存在即停止、Deployment Protection 不关闭、旧 production aliases fingerprint 前后不变；G1.2b 受限 PR/Actions 不包含这些环境 | G1.3/后续阶段按环境隔离和 Owner Gate 执行 |
| G1.2a 本地 workflow 等价验证 | 已完成 | Owner 已授权；当前 workflow 修正 commit `b0681d58`，fresh prefix/COREPACK_HOME 下 Corepack `0.34.6` + pnpm `10.33.3`，命令均退出 0 | 仅证明本地 workflow/等价合同；不替代远端 CI，详见[G1.2a 证据](../evidence/G1/2026-08-26-g1-2a-local-workflow/README.md) |
| G1.2b 远端真实 CI run | 已完成（受限） | Owner 已授权 canonical integration/PR/真实 Actions；初始 run `33027593355` / job `98372467897`、docs head `cce03acf...` 的 run `33029927182` / job `98379847069` 与 merge head `cba97eb...` 的 main run `33031297793` / job `98384190584` 均成功 | 仅证明 G1.2b 的 PR/main CI 与 merge 谱系；Preview/在线回退和 G1 Exit 仍缺失 |
| Secret/PII 值读取 | 跳过且禁止 | 安全边界要求只记录变量名/用途 | 后续由授权环境和专项审查提供最小证据 |
| 独立 PR merge 安全审查 | 已完成（GO） | 独立 reviewer 已确认远端/CI/谱系正常并正式批准 PR #1 head；随后已通过 merge commit 完成 main 写入，本次 closeout 仅归档结果，不扩大授权 | 后续 main 写入仍须独立 GO 后走非强制 PR merge；不得直接 push main、force-push、删除远端历史或改写历史 |
| 哈希 | 跳过 | 没有生成/传输物或异常覆盖疑点 | 预览交付或确定性生成物按需检查 |

## 14. 回退与恢复

- 触发条件：Node/pnpm 不一致、Git ref 不可追溯、Preview 串环境、CI 泄露变量、回退失败或 Owner 撤回 G1 授权。
- 可回退到的最后可验证状态：当前 Git 初始基线提交/tag 与 G0/P1 已通过并冻结的文档/本地原型状态；G1.1 Node22 frozen install、typecheck、lint、build 已有隔离证据；本地 G1.2a workflow 当前实现 ref 为 `b0681d58`，可回退至初版 `a388348b` 或前一 docs 基线 `f05c5f8`；G1.2b 初始 run `33027593355`、docs head run `33029927182` 与 merge head main run `33031297793` 均成功；main good ref 为 merge commit `cba97eb4e7c93e5c42ba496e4d2ddeac5b476c7c`，integration 分支可通过普通 commit/revert 回退，不 force-push 或改写历史。
- G1 开工后的回退步骤：停止晋级；保留失败 ref 与最小脱敏摘要；撤销错误 Preview 入口/变量绑定；恢复上一可验证 ref；重新检查 Node/pnpm、CI、隔离和访问边界。
- G1.3-2 回退演练入口：`bad503` 仅使用独立 `DO NOT MERGE` Draft PR，PR 永不 Ready/merge，演练结束关闭 PR 但保留分支；`good200` 另从干净 main/最终集成分支进入普通 PR。全程不 promote、不切换 Production alias、不执行 Production rollback。
- Production 不变量：Preview 前后只读核对旧 deployment `dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH` 与 aliases 的 fingerprint；fingerprint 不一致即停止并升级 Owner，不删除或覆盖历史。
- Git/版本恢复：只能切换到已记录且 Owner 可追溯的 ref；没有 ref 时标为阻塞并请求 Owner，不使用猜测性分支或覆盖性操作。
- 数据/审计/附件：本批无数据写入；不得删除审计/安全证据，不保存 secret/PII。
- 恢复后复验：Node 22/Corepack `0.34.6`/pnpm `10.33.3`、lockfile、typecheck/lint/build、环境变量来源、Preview 健康检查和回退演练均需追加记录。

## 15. 风险与债务

| 风险/债务 | 严重度 | 影响 | 临时控制 | 负责人/解除条件 |
|---|---|---|---|---|
| 根目录 Git 基线 | 已降为低 | root/main/初始 SHA 已建立；受限 canonical integration/PR/CI 已建立；当前 repo 已有 bad503 Preview；good Preview/Staging 尚无；Vercel 旧 production asset 已认证只读发现但未由当前 repo 接受或修改 | 保留初始提交和 tag；integration 后续只用普通提交或 revert | G1.3 补充 Preview/在线回退证据 |
| offline store 缺少 `@swc/helpers@0.5.23` | 已解除 | 隔离来源已完成 frozen install；原工作区恢复依赖仅用于本地预览 | 保留首次离线失败与补验记录，不改 lockfile | 后续依赖变更须在固定工具链重新验证 |
| Node/Corepack/pnpm 工具链脚本复验未完成 | 已解除 | fresh exact Corepack `0.34.6` + pnpm `10.33.3` 隔离 frozen install 后 typecheck/lint/build 均通过；初始远端 run 及更新后 docs head check 也成功；Vercel 规则要求 `engines.node=22.x` 覆盖 project Node24，默认不 PATCH | 证据仍不覆盖 Preview 部署；必须以 Preview deployment 的 provider-resolved config `builds[0].config.nodeVersion=22.x` 为主证据，build log 仅作补强；resolved 缺失或非 22 立即 STOP | G1.3/后续环境各自复验 |
| G1.2b PR/CI/main merge 已完成 | 中 | 远端 PR/Actions 与 main push 成功不等于 G1 Exit；Preview/在线回退和 Owner 签署仍缺失 | 保留 integration 分支；main 后续只通过独立 GO 后的非强制 PR merge 维护；不得直接 push main、force-push、删除远端历史或改写历史 | G1.3 Owner Gate、Preview/在线回退和 G1 Exit 签署 |
| bad503 Preview 已实施并有脱敏 READY/health 证据；good Preview/在线回退尚未实施；当前 repo 已有 bad503 Preview；good Preview/Staging 尚无，Production 未获验收 | 高 | 可能串环境或误写生产；Node、变量存在性、保护和 alias 不变量若未实证则不能晋级 | G1.3-0 本地四环境边界合同、G1.3-1 provider 只读盘点、G1.3-2 高风险审查、变量名/target 门、Deployment Protection 不关闭、旧 aliases fingerprint 前后核对 | G1.3 Owner Gate；good200 本地验证与后续 Preview/回退门；Staging/Production 由 G2-A1/P2/P7/Production 另行实施 |
| `.env.example` 仅有变量名级证据 | 中 | 无法证明任何 A1 环境可用或安全 | 不读取值、不创建项目、不接 Auth | G2-A1 在独立测试环境专项验证 |
| CI/Preview 回退未演练 | 中 | 无法证明失败后可恢复 | G1 未开工前不晋级；预览必须保留上一 ref | G1 完成回退演练并追加记录 |

## 16. 维护与运维

- 监控/日志/告警：本批无服务；G1 只规定 CI/Preview 最小日志脱敏，实际监控由后续环境合同负责。
- 备份/恢复：本批无持久化数据；G1 只验证版本/Preview 回退，不替代数据库备份恢复。
- 依赖和版本：维护 `.node-version`、`package.json` engines/packageManager、lockfile、Corepack `0.34.6` exact bootstrap 和 CI 版本约束；任何变更需 Owner/阶段记录，不使用 `latest` 或 `0.35`。
- 文档/记录维护者：15 是唯一当前状态源；本记录保存 G1 事实和追加纠正；阶段索引只做导航。
- 已知未实施环境：G1.2b PR/Actions 已运行并以 merge commit 写入远端 main，main push 质量 run 已留存证据；bad503 Preview 已有真实 READY/health 脱敏证据，good200 Preview 尚未创建；当前 repo 尚无 good Preview/Staging，Production 未获验收；Vercel 旧 production asset 已认证只读发现但未由当前 repo 接受或修改；真实 Auth/数据库仍未连接；G1.2a、G1.2b、G1.3-0、G1.3-1 与 G1.3-2 高风险独立审查均已完成各自记录，本地安装 warning 未执行 approve-builds。

## 17. Owner Gate

- Gate 名称：G1 工程底座与环境隔离 Entry Gate + Exit Gate
- Entry Gate（启动门）：GOV-1 已通过；G0/P1 当前已维持 Owner 最新“通过并冻结”。Owner 于 20:14:15 CEST 的历史 Entry 授权和 2026-08-25 23:06:57 CEST 原话 `分类目录IA通过，G0重新冻结，未覆盖项进入后续专项，恢复G1授权` 均保留；本轮按 Owner 原话 `继续下一步` 执行项目根 Git/main 和 Node22 基线。该授权不扩大为远端、Preview、生产或 G1 Exit 通过。
- Exit Gate（完成门）：G1.1–G1.3 执行完成后，必须提供 Git root/ref/回退基线、Node 22、Corepack `0.34.6`、pnpm `10.33.3`、lockfile 安装、CI `typecheck`/`lint`/`build`、Local/Preview/Staging/Production 隔离、变量名/secret 边界、Preview 部署门和回退演练证据，并经 Owner 明确通过；只有 Exit Gate 通过后才可打开 G2-A0。
- 当前门状态：Entry Gate 已恢复；G1.1 已完成 Git 基线、隔离 frozen install、typecheck、lint、build；G1.2a 本地 workflow/等价验证与 G1.2b 受限真实 PR/Actions/main merge 均已完成；G1.3-0 本地环境隔离/回退预检、G1.3-1 provider/CLI/部署前隔离预检与 G1.3-2 高风险独立审查均已完成，修正版 G1.3 Owner Gate 已于 2026-08-27 10:33:59 CEST 批准，bad503 Preview 已真实 READY/health 脱敏通过；good200 本地候选验证已通过，exact-head Actions/独立 Preview deploy 未执行，在线回退仍缺；本轮 G1 Exit requirement-to-evidence 预检结论为 NO-GO，Exit Gate 未通过。G1 当前状态为“执行中（G1.1 已完成，G1.2a 已完成，G1.2b 真实 PR/CI 已完成并合并 main，G1.3-0、G1.3-1 预检与 G1.3-2 审查已完成，修正版 Owner Gate 已批准，G1.3 implementation 执行中）”。
- Owner 决策：本轮按已批准范围采用项目根为 Git 根、`prototype/` 为 app 目录；Owner 已按一次性范围授权 canonical repo `kyox215/REBUY_SHARE` 的双 parent `integration/g1-2b`、PR #1 和真实 Actions，并在独立复审 GO 后以 merge commit `cba97eb4e7c93e5c42ba496e4d2ddeac5b476c7c` 合并 main；main push run `33031297793` / job `98384190584` 成功，integration 分支仍保留。初始 SHA、repo-local identity、Node22/Corepack `0.34.6`/pnpm `10.33.3` fresh 依赖验证及 workflow 修正 commit `b0681d585cabe2f5f293779fc3627e2782be9fa2` 均已记录；另见 §18，Owner 于 2026-08-27 10:33:59 CEST 以 `我批准 ，下次无需我批准` 批准受限 G1.3 implementation；不得扩展到新付费资源、Supabase project/cost、secret/env 值、Auth/DB/PII、支付、Staging/Production 或真实业务数据。
- 决策时间（Europe/Rome）：G0/G1 Entry 恢复为 2026-08-25 23:06:57 CEST
- 决策记录链接：[G0 阶段记录](./G0-P1-视觉验收与UI冻结.md)、[15 台账](../15-项目状态与阶段台账.md)
- 下一阶段或下一动作：bad503 Preview 脱敏结果已归档；good200 本地验证已通过；当前下一动作是提交并运行 exact-head good200 Actions，成功后进入独立 Preview deploy 门禁并补充在线 bad→good 回退证据，仍待另行证据和 Owner 验收；G1.3-0、G1.3-1 与 G1.3-2 审查本身均不是在线实施。默认不 PATCH project Node24，必须以 Preview deployment 的 provider-resolved config `builds[0].config.nodeVersion=22.x` 为主证据，build log 仅作补强；resolved 缺失或非 22 立即 STOP；provider `rootDirectory=prototype` 保持不变，从精确 archive ref 的仓库根执行 CLI，禁止从 archive/prototype 执行以免 `prototype/prototype`；最多三个 Target=Preview（bad503/good200/final），Preview 前只列变量名/target，任一应用变量存在即停止；不关闭 Deployment Protection，使用 `vercel curl`；旧 production deployment/aliases fingerprint 前后不变。完成 Preview、在线回退和 G1 Exit Gate 并经 Owner 明确通过后才打开 G2-A0；当前不连接 Supabase、数据库、Auth 或 production，G1.2b 的 canonical remote/PR/Actions 授权不向这些能力扩展。
- G1.3-2 推荐 Owner 授权语句（Owner Gate 批准前历史候选原文，已废止、禁止执行；实际批准记录见 §18）：`批准进入G1.3-2 Preview执行：确认 Vercel team/project 与 Pro 用量；project Node24.x 默认不 PATCH，依据 package.json engines.node=22.x 请求 Node22，并以新的 build log 实证实际 Node22；仅从已通过 CI 的精确 archive main@af6d7419956ce6640c0b4af5df4db0369e793f77 的仓库根执行 link/deploy，provider rootDirectory=prototype，禁止从 archive/prototype 执行以免 prototype/prototype，Target=Preview；Preview前只核对变量名与target、不读取值，任一应用变量存在即停止；不关闭 Deployment Protection，使用 vercel curl；部署前后核对旧 production deployment dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH 与 aliases fingerprint 不变；bad503 使用 DO NOT MERGE Draft PR，永不Ready/merge，关闭PR但保留分支，good200 及后续永久 good route 另从干净 main/最终集成分支进入独立普通PR；不得改变 Production alias，不注入 Supabase/Auth/DB/PII 或任何环境值，不接 Staging、Production 或真实业务数据。`

`未开始` 和 `规划` 不等于已授权执行。没有 Owner 决策、阶段记录和可追溯证据，不得把 G1 标成执行中或已通过。

## 18. 追加记录与纠正

### 2026-08-25｜G1 预编制合同

- 事实：基于只读检查记录根目录无 `.git`、Node `v20.20.2`、pnpm `10.33.3`、`.node-version=22`、package engines/packageManager/scripts/关键依赖、`pnpm-lock.yaml` 和 `.env.example` 变量名。
- 影响：确认 G1 首要缺口是 Git/版本基线、Node 20→22、四环境隔离、CI 和 Preview 回退；当前不打开 G1。
- 处理：新建本预编制合同，并由 15/stages 索引导航；不改代码、配置、依赖、服务器、数据库或外部环境。
- 新证据/Owner 决策链接：[15 G1 台账](../15-项目状态与阶段台账.md)、[14 G1 合同](../14-全局执行总计划.md)。
- Owner Gate：N/A；G0/P1 仍待 Owner 验收，G1 仍未开始。

### 2026-08-25｜G0 修订重新打开，G1 Entry 暂停

- 历史事实：Owner 于 20:14:15 CEST 原话 `G0视觉通过并冻结，授权进入G1`，该 Entry 授权保留在历史记录中。
- 新修订：Owner 随后针对分类页 `REBUY / CATALOG` 原话要求 `整个项目当中不要出现类似这样的目录，我要的是移动端app的那种体验`。
- 当前状态：G0/P1 冻结重新打开并执行修订；G1 仍“未开始”，Entry 进入暂停，Exit 未开始。不得继续任何 G1 工程动作。
- 本条只同步阶段合同与边界，不初始化 Git、不修改源码/配置、不创建 CI/环境、不连接数据库/Auth/Preview/Production；待新的 G0“通过并冻结”决定后再追加 Entry 记录。

### 2026-08-25 23:06:57 CEST｜G0 重新冻结，G1 Entry 恢复，完成只读基线

- Owner 最新正式决定原话：`分类目录IA通过，G0重新冻结，未覆盖项进入后续专项，恢复G1授权`。
- 当前状态：G1 为“准备中（Entry 已恢复授权）”。本条只记录 Entry 基线准备，不表示 G1 工程已开工或 Exit Gate 已通过。
- 只读事实：项目根与 `prototype/` 均无 `.git`；当前 `node`/`pnpm`/`corepack` 来自 `/Users/kyox215/.nvm/versions/node/v20.20.2/bin/`；当前 Node `v20.20.2`；pnpm `10.33.3`；`/Users/kyox215/.nvm/versions/node/v22.12.0/` 已安装；`prototype/.node-version=22`；package engines 为 Node `22.x`、pnpm `10.33.3`，packageManager 为 `pnpm@10.33.3`；lockfileVersion `9.0`；未发现 CI/部署候选配置；只观察 `prototype/.env.example` 两个变量名，未读值。
- G1.1 推荐与待决策：项目根作为唯一 Git 根，`prototype/` 作为 app 目录；Owner 仍需作出物质选择。确认后先建立本地 Git/ref 与 Node 22/pnpm `10.33.3` 基线，再另行决定远端/Preview，不凭空写 commit ref。
- 实际检查见[G1 Entry 基线证据](../evidence/G1/2026-08-25-entry-baseline/README.md)。未初始化 Git、未切换 Node、未创建 CI/Preview、未连接远端/Supabase/production；未修改 prototype、依赖、服务器、数据库或部署。
- 验证/跳过：本批只运行路径、版本、package、lockfile、CI/部署候选和变量名只读检查；未运行 build/lint/typecheck/E2E/browser/deploy/hash。G1.1 开工后必须在 Node 22/pnpm `10.33.3` 下补齐版本、CI、四环境和回退证据。
- Owner Gate：Entry 已恢复；Exit 未通过。下一动作是 Owner 确认 Git 根并授权建立本地基线；G2-A0 仍未开始，数据库/Auth/支付/生产仍未授权。

### 2026-08-25｜G1.1 本地 Git/Node 基线执行与离线安装阻塞

- Owner 原话：`继续下一步`。按已批准范围执行项目根唯一 Git 根、`prototype/` app 目录；不添加 remote，不创建 CI/Preview，不连接 Supabase/Auth/数据库/production。
- Git 结果：`git init -b main` 成功；repo-local `user.name=Hexiang Huang`、`user.email=kyox120@gmail.com`；分支 `main`；初始提交 `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb`；初始暂存集 79 个文件，路径/敏感边界通过。
- 工具链结果：明确 PATH 下 `node --version=v22.12.0`、`pnpm --version=10.33.3`。首次 offline install 因无 TTY 拒绝清理 modules；`CI=true` 重试后缺少 `@swc/helpers@0.5.23` tarball，退出 `ERR_PNPM_NO_OFFLINE_TARBALL`。
- 验证边界：install 失败后按要求停止依赖验证；失败前既有 node_modules 上 typecheck/lint 曾通过，但不视为失败 install 后复验。相同 source/config/lockfile 的隔离 Node22 build 证据可条件复用；本批不重跑 build。
- 当时状态（历史快照）：G1.1 Git 基线部分完成但 Node22 frozen install 验证阻塞；G1.2/G1.3 未开始；Exit Gate 未通过，G2-A0 不打开。证据：[G1.1 本地基线](../evidence/G1/2026-08-25-g1-1-local-baseline/README.md)。

### 2026-08-25｜G1.1 Node22 可复现安装与脚本验证完成

- 验证源：初始源码提交 `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb`；使用 `git archive` 解出 `/private/tmp/rebuy-g1.ml6qyC/prototype` 隔离副本，未复制当前工作区 `node_modules`。
- 实际命令与结果：明确 Node22 PATH 下 `node --version=v22.12.0`、`pnpm --version=10.33.3`；`pnpm install --frozen-lockfile`、`pnpm typecheck`、`pnpm lint`、`pnpm build` 均退出 0。安装输出提示 pnpm 忽略 `unrs-resolver@1.12.2` build scripts；未执行 `pnpm approve-builds`。
- 影响：解除此前 `@swc/helpers@0.5.23` offline 缓存缺口和 Node22 脚本复验阻塞；G1.1 已完成。G1.2 CI 与 G1.3 四环境/Preview 回退未开始，G1 Exit 未通过，G2-A0 不打开。
- 边界：未创建 remote、CI、Preview、Staging 或 Production；未连接 Supabase/Auth/数据库；未读取 secrets/PII；隔离 build 未写入工作区。验证期间的 Node22 本地预览曾返回 HTTP 200，但执行代理 session/PID 仅属临时运行态，不属于 G1.1 完成或回退证据；后续以实时 HTTP/浏览器复核为准。
- 证据：[G1.1 本地基线证据](../evidence/G1/2026-08-25-g1-1-local-baseline/README.md)。

### 2026-08-26｜本地预览运行态运维交接

- 纠正：本记录此前提及的具体 dev session 仅是验证期间的临时运行态；历史 HTTP 200/Node22 事实保留，不把 session/PID 写成 G1.1 完成或回退证据。
- 交接：后续预览使用前必须实时检查 127.0.0.1:3000 并由浏览器复核；不依赖旧 session/PID。该注记不改变 G1.1 tag、源码、隔离构建结论或阶段状态。

### 2026-08-26｜G1.2 最小 CI preflight 完成，待 Owner Gate（历史快照）

- 当时阶段（历史快照）：G1.1 已完成；G1.2 拆分为 G1.2a 本地 workflow 候选/等价验证与 G1.2b 未来远端真实 CI run。当时 G1.2 仅完成规划/preflight，不是 CI 创建或通过。
- 预检内容：推荐 GitHub Actions、固定 `ubuntu-24.04`、Node `22.12.0`、pnpm `10.33.3`/Corepack、`prototype` working-directory、15 分钟 timeout、`pull_request` + `push main`、`contents: read`，不使用 `pull_request_target`，首次关闭 package-manager cache，不注入 secrets，不 curl health、不上传 artifact、不部署。
- 动作版本边界：2026-08-26 官方仓库示例记录 `actions/checkout@v7` / `actions/setup-node@v7`；真正实施前必须从官方仓库核对 v7 release 并固定完整 40 位 commit SHA，不能凭空写 abbreviated SHA。
- warning 与失败策略：保留 `unrs-resolver@1.12.2` ignored build-script warning，不执行 approve-builds/allowlist；install/typecheck/lint/build 任一步失败即停止且不生成 Preview 晋级信号。未来依赖需要 build script 时另开供应链审查。
- 当时边界（历史快照）：未创建 `.github/`、remote、分支、tag、Preview、Staging、Production；未修改 prototype/package/lockfile；未读取 env/secrets/PII；未运行 G1.2a workflow 等价验证或 G1.2b 远端 CI。
- 证据与 Owner 推荐授权：[G1.2 preflight 记录](../evidence/G1/2026-08-26-g1-2-ci-preflight/README.md)。推荐语句：`批准进入G1.2a：采用GitHub Actions候选，在本地创建只读最小CI工作流并做本地等价验证；暂不添加remote、不push、不运行远端CI、不部署Preview、不接Supabase或生产。`
- 当时 Gate（历史快照）：G1.2 preflight 已完成，待 Owner Gate；G1 Exit 未通过，G2-A0/G2-A1/P2–P8 不打开。

### 2026-08-26｜Owner 批准 G1.2a，本地 workflow/等价验证完成（初版历史快照）

- Owner 原话：`批准进入G1.2a：采用GitHub Actions候选，在本地创建只读最小CI工作流并做本地等价验证；暂不添加remote、不push、不运行远端CI、不部署Preview、不接Supabase或生产。`
- 变更：新增唯一 workflow `[prototype-quality.yml](../../.github/workflows/prototype-quality.yml)`，commit `a388348b81300ca00f669d0bd62b0748b9f191a5`；未修改 `prototype/**`、package 或 lockfile。
- 合同：`pull_request` + `push main`、`ubuntu-24.04`、Node `22.12.0`、Corepack/pnpm `10.33.3`、`prototype` working-directory、15 分钟、`contents: read`、cache off、固定 action SHA；install → typecheck → lint → build，失败停止。
- 验证：静态 YAML/策略检查通过；隔离副本 Node `v22.12.0`、Corepack `0.29.4`、pnpm `10.33.3` 下 frozen install、typecheck、lint、build 均退出 0；保留 `unrs-resolver@1.12.2` ignored build-script warning，未执行 approve-builds/allowlist；失败停止 harness 退出 1 且 sentinel 未执行。
- Corepack：无缓存探针仅因沙箱 registry DNS `ENOTFOUND registry.npmjs.org` 失败；临时缓存复验成功，未绕过签名、未改全局 Node、未安装 `pnpm/action-setup`。
- 边界：无 remote、push、远端 CI、Preview、Supabase、数据库或生产；`G1.2b` 待 Owner Gate，G1 Exit 未通过，G2-A0 不打开。详见[G1.2a 接续证据](../evidence/G1/2026-08-26-g1-2a-local-workflow/README.md)。

### 2026-08-26｜主审修正 Corepack exact bootstrap，G1.2a fresh 等价验证重验

- 纠正范围：此前 `a388348b` 初版记录的 Node22 bundled Corepack `0.29.4` warmed-cache 成功只保留为历史诊断。无缓存 `ENOTFOUND registry.npmjs.org` 发生在签名检查前，不能排除 npm 新签名 key/签名链或兼容失败，不能写成 fresh runner 获取安全证明；参考[Corepack issue #627](https://github.com/nodejs/corepack/issues/627)、[issue #612](https://github.com/nodejs/corepack/issues/612)。
- 当前实现：workflow 修正 commit `b0681d585cabe2f5f293779fc3627e2782be9fa2` 在 `${RUNNER_TEMP}/corepack` 临时 prefix 中精确安装 Corepack `0.34.6`，通过 `${GITHUB_PATH}` 供后续步骤调用；不覆盖 runner Node 目录，不使用 `latest`、Corepack `0.35`、`pnpm/action-setup` 或完整性绕过。
- fresh 等价验证：Node `v22.12.0`、npm `10.9.0`、全新 npm cache/`COREPACK_HOME`/prefix；npm exact Corepack 安装退出 0，package version `0.34.6`，engines `^20.10.0 || ^22.11.0 || >=24.0.0`；Corepack `0.34.6` 与 pnpm `10.33.3` 解析均退出 0；frozen install 退出 0（351 packages，复用 351、下载 0），typecheck/lint/build 均退出 0，Next `16.3.2`。
- 当前 Gate：G1.2a fresh workflow/等价验证已完成；G1.2b 待 Owner Gate；仍无 remote、远端 CI、Preview、Supabase、数据库或生产连接；G1 Exit 未通过，G2-A0 不打开。若 npm registry、签名链或 exact bootstrap 失败，保留最小脱敏错误并回退 workflow commit，不静默换版本。

### 2026-08-26｜G1.2b-0 远端目标只读审计完成，G1.2b 待 Owner Gate

- 审计范围：只读核验 `gh` 登录账户、候选公开仓库、Actions 元数据、默认分支 tip，并在临时仓库比较本地 `main` 与候选远端 `main`；未添加 remote、未 push、未触发 CI、未读取 secrets、未修改 workflow 或 `prototype/**`。
- Owner 边界：现有 G1.2a 原话只授权本地 workflow/等价验证并明确暂不添加 remote、push、远端 CI；本批没有新增 G1.2b Owner 授权，因此仍保持待 Gate。
- 结果：最可能候选为 `kyox215/REBUY_SHARE`（public/main、未归档/禁用、非 fork），远端 tip `366ad7f287a00f795c742d7f2df10a531fa42e7c`；公开 workflows/runs 均为 0；Actions permissions 因 gh token 无效返回 401，记为 unknown。
- 历史关系：本地 `099439c460584a9fda3bb7b216639181786b3fa9` 与远端无共同祖先；提交计数 `15/3`、文件树 `83/46`、差异 `A=0/D=37/M=16/R=0`，不可 fast-forward 或直接覆盖；完整证据见[G1.2b-0 远端目标审计](../evidence/G1/2026-08-26-g1-2b-0-remote-target-audit/README.md)。
- 当前 Gate：G1.2b 仍待 Owner Gate；Owner 需先确认 canonical repo、历史策略、remote/fetch、分支/PR、push 和真实 Actions run 是否授权；G1 Exit 未通过，G2-A0 不打开。

### 2026-08-26｜G1.2b-1 本地历史保全与发布前敏感审计演练完成，G1.2b 待 Owner Gate

- 审计范围：在 `/private/tmp` 临时 clone 候选 `kyox215/REBUY_SHARE`，将本地 `main` fetch 到独立 ref，构造双 parent 历史保全候选；双方完整可达 commit/tree/blob 均做路径、内容和大小审计。临时目录已清理，当前仓库未写入 remote。
- 演练结果：临时 rehearsal `134b7396758792806e0e9016c924c632cd4eedea` 的 parent 1 为远端 `366ad7f287a00f795c742d7f2df10a531fa42e7c`、parent 2 为本地 `82e1090feeefb6717f8e972a0e889312f2167910`，tree `c6e69cbeb2709e3c2a7d595ba8ce5595efa99d2f` 与本地一致；两侧均为祖先，候选相对远端差异 `A=38/M=16/D=0/R=0`。该 SHA 仅用于演练，不得未来复用。
- 完整历史审计：local `16 commits/147 blobs`、remote `3 commits/51 blobs`，路径风险均为 `0`；双方各 1 条 `runtime_field_reference`，人工分类为运行时配置字段引用而非真实 secret value；最大 blob `1,275,681 bytes`，20 MiB 超阈值均为 `0`。候选 tree 中 workflow 存在，`pull_request`/`push main` 与 `contents: read` 静态断言通过。
- 当前 Gate：G1.2b 仍待 Owner Gate；仓库仍是候选，未 push/开 PR/运行远端 CI；G1 Exit 未通过，G2-A0 不打开。Owner 需确认 canonical repo、无共同祖先的历史策略、integration 分支/PR、push 与真实 Actions run 授权。详见[G1.2b-1 演练证据](../evidence/G1/2026-08-26-g1-2b-1-local-integration-rehearsal/README.md)。

### 2026-08-26｜G1.3-0 本地环境隔离、忽略规则与回退预检完成，G1.3 实施未开始

- 预检范围：只读核对根 `.gitignore`、`prototype/.env.example` 变量名/空值合同、`.node-version`、packageManager/engines、docs/11 与 G1 合同；使用 `git check-ignore` 验证环境/构建状态路径；未读取 `.env` 值、shell secret、外部凭据或 provider 状态。
- 当前事实：`.env`、`prototype/.env.local`、`.vercel/**`、`.supabase/**`、node_modules、`.next`、pnpm store、tsbuildinfo 均按规则忽略，`prototype/.env.example` 可跟踪；Git 当前 tree/index 无非 example 环境、部署状态、密钥证书、dump、附件或临时风险路径。ignored 运行态只记录类别/数量，不进入提交。
- 四环境边界：Local 使用 `main` 当前 ref、Node `v22.12.0`/Corepack `0.34.6`/pnpm `10.33.3`、合成数据且无外部连接；Preview 未建立；Staging 未打开；Production 关闭于本地工作范围。外部资源不能由本地未绑定事实推断不存在，未有认证实时证据的状态保持 `unknown`。
- 版本取回：在 `/private/tmp` 对当前 `bfef296...` 与 workflow ref `b0681d...` 分别 `git archive`；两者 `prototype` tree 均为 `48dd99...`、workflow blob 均为 `7f2dbd...`，archive 与解包比较通过；docs 差异为预期，未 checkout/reset 当前分支。临时目录已清理。
- 当前 Gate：G1.3-0 preflight 已完成，但未部署 Preview、未在线演练回退、未创建 Staging/Production；G1.3 实施未开始，G1 Exit 未通过，G2-A0 不打开。未来需 Owner 单独批准环境资源、变量/访问/数据边界、部署入口、health 与回退命令后方可执行。
- 证据：[G1.3-0 本地环境预检](../evidence/G1/2026-08-26-g1-3-0-local-environment-preflight/README.md)。

### 2026-08-26｜G1 Exit 本地预检完成，NO-GO，待 Owner 明确

- 预检范围：只读核对当前 `main`/HEAD、稳定 tag、G1.1–G1.3-0 evidence、最小 workflow、G1 合同与 Owner 清单，建立完整 requirement-to-evidence 矩阵；未修改 prototype、workflow、配置或 Git 外部状态。
- 结果：G1.1 本地基线、G1.2a fresh exact bootstrap/workflow 等价验证、G1.3-0 本地隔离与 archive 预检已具备；G1.3-0 已满足 Staging/Production 边界合同，实际资源与高风险能力属于后续专项；G1.2b 真实 GitHub Actions run、canonical repo/权限确认、Preview 实际部署、在线 bad ref→good ref 回退及 Owner Exit 签署仍缺失或为 unknown。
- 当前 Gate：保持 G1“执行中（G1.1 已完成，G1.2a 已完成，G1.2b 待 Owner Gate）”；G1.3 实施未开始；G1 Exit 未通过，G2-A0 不打开。当前结论与下一步详见[G1 Exit 预检证据](../evidence/G1/2026-08-26-g1-exit-preflight/README.md)及[G1 Owner 验收清单](./G1-Owner验收清单.md)。
- 边界：本条不代表远端 CI、Preview、Staging、Production 或在线回退已运行，不产生 Owner Exit 通过日期或签署 ref；后续仍须在对应 Owner Gate 下执行。

### 2026-08-26｜独立审查纠正：G1 scope 与 G1.2b 授权边界

- 纠正：独立审查发现上一条 G1 Exit 预检把 Staging/Production 实际实施混入 G1 缺口，并把 G1.2b“只读认证”与后续写入动作写成不清晰边界。本条将 G1.3-0 已满足的四环境隔离合同，与后续 G2-A1/P2/P7/Production 实施、外部 provider `unknown` 盘点分开记录。
- 当前范围：Staging/Production 实际资源、Auth/DB/RLS/PII、监控、备份/恢复不属于 G1 Exit 前置；外部 provider/资产认证只读盘点仍须在选择 Preview provider 前完成，但不授权创建或连接资源。
- G1.2b 授权入口：只有 Owner 明确采纳[G1 Owner 验收清单](./G1-Owner验收清单.md)与[G1 Exit 预检证据](../evidence/G1/2026-08-26-g1-exit-preflight/README.md)中的精确语句后，才解除一次受限 integration/PR/Actions 流程的旧 remote/push 禁止；当前 G1.2b 仍待 Owner Gate，G1 Exit 未通过，G2-A0 不打开。

### 2026-08-27｜G1.2b 真实 PR/CI 实际执行结果（合并前历史快照）

- Owner 授权边界：允许将当前仓库源码、项目文档、Git 历史和图片公开上传至 `kyox215/REBUY_SHARE`，推送 `integration/g1-2b`、创建 PR、运行 GitHub Actions，并在通过后以非强制方式合并；本次禁止 force-push、删除远端历史、Preview、Supabase/Auth/DB 和生产。Workflow scope 同样限定于本仓库本次工作流。
- 执行结果：canonical repo 为 `kyox215/REBUY_SHARE`；远端 `main=366ad7f...` 未漂移；integration 初始 head `f746dcacc0afc2d45b847346f20078a159c2e032` 保留远端与本地历史的双 parent；PR #1 base=`main`、head=`integration/g1-2b`，保持 OPEN/CLEAN，未合并 `main`。完整脱敏记录见[G1.2b 远端 PR/CI 证据](../evidence/G1/2026-08-27-g1-2b-remote-ci/README.md)。
- 真实 Actions：初始 run `33027593355`、job `98372467897` 均 SUCCESS，验证 head `f746dca...` 的 install、typecheck、lint、build 与收尾步骤；本批文档 commit 推送后的更新 head check 作为当前 PR 的独立复核，不递归改写初始 evidence run ID。
- 远端只读设置：Actions enabled=true、default workflow permissions=`read`、allowed actions=`all`、SHA enforcement=false、PR approval=false；selected-actions 在 all 策略下返回 409。workflow 自身保持完整 action SHA 与 `contents: read`，仓库级 all/未强制 SHA 作为后续治理风险。
- 当前 Gate：G1.2b 真实 PR/CI 已完成；G1.3-0 仍仅本地预检，G1.3 实施未开始；Preview、在线 bad ref→good ref 回退、Owner G1 Exit 签署仍缺失，故 G1 Exit 继续 NO-GO，G2-A0 不打开。未修改 `prototype/**`、package、lockfile 或 workflow。

### 2026-08-27｜G1.2b PR #1 main merge closeout

- 独立 merge reviewer 已对 PR #1 head `0bb5fb527d51a304b95b05794345bd23128e1534` 正式给出 GO。随后仅通过 GitHub PR 以 merge commit 合并，未 direct push main、squash、rebase 或 force-push；`integration/g1-2b` 未删除。
- 合并前远端 main 为 `366ad7f287a00f795c742d7f2df10a531fa42e7c`；merge commit `cba97eb4e7c93e5c42ba496e4d2ddeac5b476c7c` 的 parent 1 为该 main、parent 2 为 `0bb5fb527d51a304b95b05794345bd23128e1534`，双历史得到保留。
- main push 触发 run `33031297793` / job `98384190584`，install、typecheck、lint、build 和收尾步骤全部 SUCCESS。完整 closeout 见[G1.2b main merge 证据](../evidence/G1/2026-08-27-g1-2b-main-merge/README.md)。
- 当前 Gate：G1.2b 真实 PR/CI/main merge 已完成；G1.3-0 仍仅本地预检，G1.3 实施、Preview、在线 bad ref→good ref 回退和 Owner G1 Exit 签署仍缺失，故 G1 Exit 继续 NO-GO，G2-A0 不打开。Actions `allowed_actions=all`、SHA enforcement=`false` 治理债务仍待后续专项。

### 2026-08-27｜G1.3-1 provider/CLI/部署前隔离预检完成，G1.3 实施待 Owner Gate

- 本批只读确认现有 Vercel team `kyox120-9295's projects` / `team_AOJDnrjov0QDLqpvMyhwA1yc` / Pro 与 project `rebuy-share` / `prj_g1W3AWm3hkbZib9zDgm6YQfGEyHL`；仅有旧 `READY`、`target=production` deployment `dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH`，项目 Node `24.x`、无 Git link。本批未改项目、部署、Production alias、计费或设置。
- Supabase 只读事实为唯一 org `kyox120-9295's projects` / Pro，仅有无关项目 `ChinaTech_date` 与 `PartsPro-V4`，没有 Rebuy 项目；未连接或修改 Supabase/Auth/DB/Storage，未读取或记录 host、key、token、secret、环境值或 PII。
- 从 `main@af6d7419956ce6640c0b4af5df4db0369e793f77` 建立 `/private/tmp` 临时 archive 副本，在 Node `22.12.0`、Corepack `0.34.6`、pnpm `10.33.3` 下完成 frozen install、typecheck、lint、build，均退出 0；临时目录已清理，仅保留 `unrs-resolver` ignored build-script warning。main Actions run/job `33034314565/98393579170` 已 SUCCESS。
- Vercel CLI `53.1.1` 的 `deploy/link/inspect --help` 语法已核对，`whoami` 无可确认身份输出；未 login、link、deploy、inspect、promote、rollback，未写 `.vercel`。
- Node 结论（G1.3-2 审查前快照）：`package.json` 的 `engines.node=22.x` 是项目合同；该快照原未确认它覆盖 Vercel project Node `24.x` 设置，并要求 Owner 决定是否调整 project 默认值。随后 G1.3-2 已纠正为默认不 PATCH project Node，由 `engines.node=22.x` 请求 Node22，并以新的 build log 实证；当前仍只允许规划显式 Preview，不得进入 Production。
- 当前 Gate：G1.3-1 provider/CLI/隔离预检完成；G1.3 Preview、在线 bad ref → good ref 回退和 Owner G1 Exit 签署仍缺失，G1 Exit 保持 `NO-GO`，G2-A0 不打开。推荐仅在 Owner Gate 后以已通过 CI 的 `main@af6d741`、Root `prototype`、Target Preview 执行，按 Pro 正常 build/compute 用量观察；不得注入 Supabase/Auth/DB/PII 或环境值，不改变 Production alias。

完整脱敏记录见[G1.3-1 provider/Preview 部署前预检](../evidence/G1/2026-08-27-g1-3-1-provider-preview-preflight/README.md)。

### 2026-08-27 10:33:59 CEST｜G1.3 修正版 Owner Gate 已批准，implementation 执行中

- Owner 原话：`我批准 ，下次无需我批准`。该回复语义明确承接主线程上一条完整 G1.3 修正版授权语句，批准受限 G1.3 implementation 范围，不是 G1 Exit 签署，也不把条件 GO 候选写成已完成。
- 授权范围：允许 Vercel OAuth 临时认证；仅限上一条完整授权语句指定的两个 `codex/*` 分支/PR/Actions 入口（本记录不臆补名称或编号）；最多三个 `Target=Preview`，角色仅为 `bad503`、`good200`、`final`；允许 Vercel Pro 正常 build/compute 用量；在该范围内风险触发时可受限取消构建中的 Preview。OAuth/login 是否发生、分支/PR/Actions 是否创建及 Preview 是否部署，均须由后续证据确认。
- 原修正合同保持不变：`engines.node=22.x` 覆盖 project Node24，Node 默认不 PATCH，必须以 deployment provider-resolved config `builds[0].config.nodeVersion=22.x` 为主证据，build log 仅作补强；resolved 缺失或非 22 立即 STOP；provider `rootDirectory=prototype` 保持不变，从精确 archive 的仓库根执行 CLI，禁止从 archive/prototype 执行；Preview 前只列变量名/target，任一应用变量存在即停止；不关闭 Deployment Protection，使用 `vercel curl`；旧 production deployment `dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH` 与 aliases fingerprint 前后必须不变；bad503 使用永不 Ready/merge 的 `DO NOT MERGE` Draft PR，关闭 PR 但保留分支，good200 与后续永久 good route 另用独立干净普通 PR。
- `下次无需我批准` 仅表示上述已批准范围内的常规可回退步骤和必要重试不重复询问；不得扩展到未来新付费资源、Supabase project/cost、任何 secret/env 值、Auth/DB/PII、支付、Staging/Production、真实业务数据、Production alias/promote/rollback、项目删除或其他 team/project 设置。
- 当前状态：G1.3 implementation 从“待 Owner Gate”更新为“已授权/执行中”；当前仍未执行或不伪造任何 login、push、PR、Actions、Preview、deploy 结果，G1 Exit 保持 `NO-GO`，G2-A0 不打开。

### 2026-08-27｜G1.3 外部 provider/Preview preflight 收口（GO）

- 本批只读确认 GitHub `origin/main=af6d7419956ce6640c0b4af5df4db0369e793f77` 无漂移；latest main Actions 的 `head_sha` 与该 SHA 相符、状态 `success`，对应 run `33034314565` / job `98393579170`。
- Vercel team `team_AOJDnrjov0QDLqpvMyhwA1yc`、project `prj_g1W3AWm3hkbZib9zDgm6YQfGEyHL` 已按最小字段只读确认；default CLI existing auth 用户为 `kyox120-9295`。未创建新 OAuth token；device UI 未获授权且未记录凭据。project Node `24.x` 与 provider `rootDirectory=prototype` 均保留不变，Git link absent。
- `package.json` `engines.node=22.x` 覆盖 project Node24，正式策略为默认不 PATCH；未来 Preview 必须从 exact git archive 的仓库根执行 CLI，禁止从 archive/prototype 执行以免 `prototype/prototype`，并以每次 deployment provider-resolved config `builds[0].config.nodeVersion=22.x` 为主证据，build log 仅作补强；resolved 缺失或非 22 立即 STOP。
- Preview env 只读脱敏结果为 `0`；未读取或记录任何 value。Deployment Protection 为 `Standard` + `Require Log In`，未关闭或改弱。
- 旧 production deployment `dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH` 仍为 `target=production`、`READY`，latest production ID 相同；production aliases 数量 `2`，排序后 SHA-256=`c06eeb6c408c562d7d6906cf1ccd71776beea381afae47e94547c696133f79aa`。不记录 alias 字符串或 URL。
- G1.3 external preflight 与 rehearsal Entry 门为 `GO`，仅表示 provider/隔离/执行入口可按合同继续；不表示 Preview 已部署、在线 bad→good 已完成或 G1 Exit 通过。G1 Exit 继续 `NO-GO`，G2-A0 不打开。
- 未执行或伪造 login、push、PR、Actions、Preview、deploy 结果；未做 link、项目设置、env、Protection、alias、promote、rollback 或其他外部写入。临时 env 读取目录 `/private/tmp/rebuy-g13-preview-env-read-20260827-1050` 保留且不含敏感值记录。

### 2026-08-27｜bad503 Preview 验收 GO，good200 本地验证通过

- bad503 Preview 已按受控批次完成并验收 GO：deployment `dpl_J9E3WThCmtqxAndfjQDKAW1G49EU` 为 Target=Preview/READY，resolved config 为 `builds[0].config.nodeVersion=22.x`；根页 HTTP `200`，health HTTP `503`，body 为 `status: unhealthy`，Cache-Control 含 `no-store`；Deployment Protection 保持 Standard + Require Login；旧 Production asset、alias count=`2` 及 fingerprint 均保持不变。
- good200 仅完成本地候选：Node `22.12.0`、Corepack `0.34.6`、pnpm `10.33.3` 下 frozen install、typecheck、lint、build 均退出 `0`；本地 root/health 均 HTTP `200`，body 仅 `{"status":"healthy"}`，Cache-Control 含 `no-store`。good commit、PR、Actions、Preview 尚未开始。
- 下一步是提交 good 候选、普通非强制 push、创建独立 PR 并运行 Actions；Actions 成功后才进入 good Preview deploy 门禁。当前 G1 Exit 仍为 `NO-GO`。
