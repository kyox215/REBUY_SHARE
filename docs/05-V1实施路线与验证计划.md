# V1 实施路线与验证计划

文档状态：Owner 里程碑与验证合同  
当前状态（截至 2026-08-28，Europe/Rome）：G0/P1 已通过并冻结；G1 已完成并通过 G1-19/G1 Exit=GO（验收 ref=`d51f1c7cb47e2fe2932b29bd39420f5d092a8160`）；G2-A0 为 Exit GO（验收 ref=`140ea15d9c3f178a326709d35ad1750a156df0d1`），且远端 docs-only reconciliation 已完成：PR #7 merge=`fd9b712c7b07bf34399f9838eebb75846425c1d1`，parents=`7ea1e45ad22ab29105910665baf4bbd7212241c5` + `1433e7c7c141df0f5498fff7cd645a8d5c92340c`，main Actions run=`33122238997`/job=`98691703085` 全绿，GitHub deployments=0，来源分支保留；这些是 docs-only/CI 事实，不等于 Auth/DB/运行时通过。G2-A1 技术阶段尚未开始；最小资源存在性/基础预检已完成；完整 resource/cost/secret Gate 仍关闭；P2–P8 未打开。当前唯一状态以 [15 台账](./15-项目状态与阶段台账.md) 为准。G0/P1 未覆盖的直接 Enter 键盘提交仍为后续专项，不阻塞冻结。
验证原则：按风险匹配最小验证；全量测试、完整构建和 E2E 只在确实覆盖新增风险的里程碑或发布候选执行，不在每个小阶段重复。

## 1. 验证等级

| 风险 | 最小验证 | 额外门槛 |
|---|---|---|
| 低：文档、文案、局部静态内容 | 局部验证：文件存在、链接、标题、围栏、目标页面检查 | 无需全量测试 |
| 中：共享组件、权限合同、跨页面交互 | 综合验证：受影响测试、类型/构建、关键浏览器路径 | 一次综合审查 |
| 高：生产、数据库、支付、隐私、迁移、跨租户写入 | 专项验证：专项审查、负向测试、回滚证据、监控/备份检查 | Owner 明确批准后才可推进 |

每个阶段都保留范围、证据、Owner 决定和回退点。实现代理不得把本地演示、静态检查或合成数据描述成生产验收。

独立审查触发器：凡涉及数据库/schema/RLS、身份与权限、支付、隐私、生产写入或跨组织数据的阶段，必须由独立审查完成并保留结论、负向证据和回退证据；P1 文档和本地视觉原型不连接这些系统，也不因此提前启动高风险审查。

## 2. 分阶段路线

### P0 文档基线

| 项目 | 内容 |
|---|---|
| 范围 | README、00–06 的文档基线已完成；统一多商家 V1、角色、架构、阶段和 P1 原型合同 |
| 依赖 | 当前批准的产品决定、01/02 设计基线 |
| 风险等级与主要风险 | 低：历史较窄方案或旧原型重新成为隐含范围 |
| 最小验证 | 00–06 七个文档存在且非空；README 的七个相对链接可解析；标题和要求章节存在；围栏成对；反向微信功能表述和真实 PII 检查 |
| Owner Gate | P0 文档基线已形成并可作为后续执行入口；后续治理和阶段状态以 14/15 继续记录，不把文档存在写成产品或生产通过 |
| 回退方式 | 只修订本轮新增文档或记录决定；保留既有 01/02，不回退无关修改 |

### P1 本地客户端视觉原型

| 项目 | 内容 |
|---|---|
| 范围 | 移动优先、桌面响应式的首页、分类/搜索、详情、购物车/结算、订单、我的；演示五项导航、搜索筛选、游客/零售/批发样本、普通/二手差异、多商家批次/子订单、深色和三语 |
| 依赖 | 01、02、06；虚构商品/商家/订单和可识别视觉资产 |
| 风险等级与主要风险 | 中：原型被误认为生产功能；身份样本被误认为手动价格切换；信息溢出或二手事实缺失 |
| 最小验证 | 相关 lint/typecheck/build；移动和桌面页面检查；关键点击路径；无重叠/溢出；控制台无阻塞错误 |
| Owner Gate | 2026-08-26 文档中的历史 Gate 摘要：当时 Owner 决定将分类目录 IA 更新为通过并冻结并恢复 G1 Entry；点击搜索已通过，直接 Enter 键盘提交未验证成功并转入后续专项。当时的 G1 Entry 顺序不代表当前状态；当前 G1 Exit=GO、G2-A0 Exit GO 且远端 docs-only reconciliation 已完成、G2-A1 技术阶段尚未开始且最小资源存在性/基础预检已完成、完整 resource/cost/secret Gate 仍关闭、P2–P8 未打开，以 [15 台账](./15-项目状态与阶段台账.md) 为准 |
| 回退方式 | 保留文档和虚构数据合同，停止或回退本地原型改动；不触及后端、数据库或生产 |

### G1 工程底座与环境隔离

| 项目 | 内容 |
|---|---|
| 范围 | Git/版本基线、Node 22 与包管理器版本固定、CI 定向 `typecheck`/`lint`/`build`、Local/Preview/Staging/Production 隔离、secret/环境变量边界、可回退版本和预览部署门 |
| 依赖 | G0/P1 Owner Gate；若根目录没有可用 Git 元数据，必须如实记录“版本基线待建立/阻塞”，不得捏造 commit、分支或发布引用 |
| 风险等级与主要风险 | 中：版本不可追溯、Node/包管理器漂移、环境串线、secret 泄露、预览不可回退或误连生产 |
| 最小验证 | 记录 Git/版本状态；固定 Node 22 与包管理器版本；CI 运行定向 typecheck/lint/build；验证四环境变量/资源边界、secret 不进仓库/日志、预览部署和回退版本 |
| Owner Gate | 当前（截至 2026-08-28）：G1 已通过 G1-19/G1 Exit=GO，验收 ref=`d51f1c7cb47e2fe2932b29bd39420f5d092a8160`；G2-A0 Exit 已 GO（验收 ref=`140ea15d9c3f178a326709d35ad1750a156df0d1`），远端 docs-only reconciliation 已完成：PR #7 merge=`fd9b712c7b07bf34399f9838eebb75846425c1d1`，parents=`7ea1e45ad22ab29105910665baf4bbd7212241c5` + `1433e7c7c141df0f5498fff7cd645a8d5c92340c`，main Actions run=`33122238997`/job=`98691703085` 全绿，GitHub deployments=0，来源分支保留；这些是 docs-only/CI 事实，不等于 Auth/DB/运行时通过。G2-A1 技术阶段尚未开始；最小资源存在性/基础预检已完成；完整 resource/cost/secret Gate 仍关闭；P2–P8 未打开。Preview 部署、Auth、数据库和 Production 仍不在本批范围 |
| 回退方式 | 关闭预览入口或回到最近可验证版本，撤销错误环境变量/部署配置，保留版本和失败证据；未建立 Git 基线时先阻塞，不绕过继续 |

### G2-A0 账号安全合同与威胁模型验收

| 项目 | 内容 |
|---|---|
| 范围 | 以 07/08/09 验收 identity、组织/店铺 membership、角色权限、批发申请与资格生命周期、邀请/恢复/撤销和威胁模型 |
| 依赖 | G1 Owner Gate；07/08/09 作为账号安全合同输入 |
| 风险等级与主要风险 | 高：跨组织访问、客户端伪造角色、资格状态混淆、邀请/会话撤销边界不清 |
| 最小验证 | 文档一致性、角色正/负向矩阵、威胁模型、PII/日志最小化、独立安全审查和回退证据 |
| Owner Gate | G2-A0 Exit 已 GO，远端 docs-only reconciliation 已完成；G2-A1 技术阶段尚未开始，最小资源存在性/基础预检已完成；完整 resource/cost/secret Gate 仍关闭，未打开 A1 技术实测；A1 的 secret、Auth/DB/Storage/OAuth/SMTP 和连接仍须独立风险 Gate，未以 P1 原型或资源健康状态代替授权证明 |
| 回退方式 | 冻结账号实现，保留只读合同和审查记录，追加修订；不得绕过 G2-A0 打开 Auth 或组织写入 |

### G2-A1 独立 Auth spike

| 项目 | 内容 |
|---|---|
| 范围 | 以 10/11 为输入，在独立 local/preview-staging 环境验证登录、callback、账号链接、邀请邮箱、TOTP、会话和停止条件 |
| 依赖 | G2-A0 Owner Gate；10/11 的 Auth 合同、连接骨架和依赖检查只能作为输入，不等于 spike 已实施 |
| 当前状态 | Auth spike 实施未开始；没有 Staging/生产 Auth、真实登录或真实 PII 证据 |
| 风险等级与主要风险 | 高：真实凭据/PII 越界、callback 劫持、会话失效不完整、测试环境误连 production |
| 最小验证 | 隔离测试账号、正/负向认证、callback/state、会话撤销、邀请/TOTP、日志脱敏、依赖构建和独立安全审查 |
| Owner Gate | G2-A1 证据和安全审查通过后才进入 P2；失败则保持独立测试边界，不创建生产身份 |
| 回退方式 | 关闭 spike 入口、撤销测试配置、保留审计/失败证据；不触碰 production 用户或数据 |

### P2 登录/组织/权限纵切

| 项目 | 内容 |
|---|---|
| 范围 | 认证、组织、成员、角色、店铺范围、批发资格和服务端授权的最小闭环 |
| 依赖 | G0/P1、G1、G2-A0、G2-A1；04 数据边界；03 权限矩阵；P1 仅提供交互参考 |
| 风险等级与主要风险 | 高：跨组织读取、客户端伪造角色、RLS 与服务端授权不一致 |
| 最小验证 | 角色正向/负向授权、RLS 隔离、DTO 脱敏、会话失效、审计和类型/相关构建 |
| Owner Gate | 通过安全/权限专项审查，确认没有浏览器 service role 和跨租户泄露 |
| 回退方式 | 禁用未通过的组织/角色路径，恢复上一个迁移和策略快照；不绕过授权继续开放 |

### P3 商家入驻与平台审核

| 项目 | 内容 |
|---|---|
| 范围 | 入驻申请、补件、审核、通过/拒绝/暂停、店铺建立和审核记录 |
| 依赖 | P2 身份和权限；审核资料、政策和 SLA 决策 |
| 风险等级与主要风险 | 高：资料过度收集、审核越权、审批状态丢失或商家被错误启用 |
| 最小验证 | 状态迁移、重复提交、私有资料访问、审核角色负向测试、审计和回滚演练 |
| Owner Gate | Owner 确认资料、责任、时限和法律/隐私处理；高风险资料需专项审查 |
| 回退方式 | 停止新申请或店铺启用，保留只读审核记录，恢复最近可验证状态 |

### P4 商品/定价/库存

| 项目 | 内容 |
|---|---|
| 范围 | 分类、商品、变体、listing、零售/批发价、起订量、阶梯价、普通数量库存和二手单件库存 |
| 依赖 | P2 权限、P3 有效商家；价格和库存业务决策 |
| 风险等级与主要风险 | 高：批发价泄露、超卖、单件重复售出、并发更新覆盖和商业数据暴露 |
| 最小验证 | 角色价格矩阵、库存并发/预留、二手单件唯一性、状态迁移、RLS、审计和受影响模块构建 |
| Owner Gate | 数据库/RLS/库存专项审查通过；没有安全证据不得进入真实库存 |
| 回退方式 | 下架问题 listing，冻结受影响库存写入，按审计事件和版本恢复；不直接删除历史记录 |

### P5 浏览/购物车/订单

| 项目 | 内容 |
|---|---|
| 范围 | 浏览、搜索、详情、身份适用价格、多商家购物车、结算校验、批次、商家子订单和订单事件 |
| 依赖 | P2 身份；P4 目录/价格/库存；地址和订单部分失败策略 |
| 风险等级与主要风险 | 高：重复订单、价格/库存变化、批次拆分错误、部分成功状态不清 |
| 最小验证 | 关键浏览器路径、幂等重试、并发库存、批次/子订单关系、失败恢复、订单权限和相关构建 |
| Owner Gate | 订单和库存专项审查；明确不能在线支付或自动退款的边界 |
| 回退方式 | 停止提交入口或切换只读查询，按请求标识查询原结果；不通过重试创建第二单 |

### P6 商家后台闭环

| 项目 | 内容 |
|---|---|
| 范围 | 商家紧凑工作台、商品/价格/库存操作、子订单处理、售后响应和商家日志 |
| 依赖 | P2–P5；商家操作职责、字段可见性和售后政策 |
| 风险等级与主要风险 | 高：商家员工越权、内部商业字段泄露、运营操作不可追溯 |
| 最小验证 | Owner/员工权限矩阵、字段级 DTO、关键状态和审计、桌面密度/键盘路径、相关测试 |
| Owner Gate | 商家业务代表和安全审查确认可操作范围；后台视觉不与买家端混淆 |
| 回退方式 | 按模块关闭写入或回到只读工作台，保留订单和库存事件，逐项恢复 |

### P7 受控试运营

| 项目 | 内容 |
|---|---|
| 范围 | 小范围、明确商家和客户、受控数据、监控、客服和回滚流程的试运营 |
| 依赖 | P2–P6 全部通过；生产环境、备份、隐私、运维和支持流程 |
| 风险等级与主要风险 | 高：真实 PII、跨租户事故、库存/订单损失、监控缺失和无法回滚 |
| 最小验证 | 发布候选完整构建/lint/typecheck/E2E、RLS 负向测试、备份恢复、监控告警、试运营演练 |
| Owner Gate | 生产、数据库、隐私、运营和安全专项审查均通过，Owner 书面批准 |
| 回退方式 | 使用已验证版本回滚，暂停写入/试运营，按事件和备份恢复；保留审计证据 |

### P8 后期支付/物流/税务

| 项目 | 内容 |
|---|---|
| 范围 | 在线支付与平台分账、退款、物流自动化、税务/电子发票等后期能力 |
| 依赖 | P7 的真实订单和责任边界；商业、法律、税务、隐私、安全和供应商决策 |
| 风险等级与主要风险 | 高：资金损失、分账错误、税务不合规、个人资料扩散和供应商故障 |
| 最小验证 | 独立支付/税务/隐私/安全专项审查、沙箱和故障演练、对账、回滚与人工兜底 |
| Owner Gate | 未完成全部专项审查不得进入 V1 生产范围 |
| 回退方式 | 关闭新支付/物流入口，保留人工处理和可核对订单，不删除交易记录 |

## 3. 当前执行状态

- P0 文档基线已完成并通过文档级检查；不代表网站、后台、后端或生产通过。
- G0/P1 原型已创建，标题层级、统一选择器与分类目录 IA 修订已完成；Owner 于 2026-08-25 作出决定，将 G0/P1 更新为“已通过并冻结”，checkpoint 16 为通过。点击搜索已通过，直接 Enter 键盘提交未验证成功并转入后续专项；门槛与历史证据见 [本地视觉原型执行合同](./06-本地视觉原型执行合同.md)、[G0 阶段记录](./stages/G0-P1-视觉验收与UI冻结.md) 与[分类目录 IA 证据](./evidence/G0-P1/2026-08-25-category-directory-ia/README.md)。
- P2–P8 不因 P1 的静态或本地预览证据自动打开。
- 截至 2026-08-28，G1 已完成并由 Owner 签署 G1-19/G1 Exit=GO，验收 ref=`d51f1c7cb47e2fe2932b29bd39420f5d092a8160`；G2-A0 为 Exit GO，验收 ref=`140ea15d9c3f178a326709d35ad1750a156df0d1`，且远端 docs-only reconciliation 已完成：PR #7 merge=`fd9b712c7b07bf34399f9838eebb75846425c1d1`，parents=`7ea1e45ad22ab29105910665baf4bbd7212241c5` + `1433e7c7c141df0f5498fff7cd645a8d5c92340c`，main Actions run=`33122238997`/job=`98691703085` 全绿，GitHub deployments=0，来源分支保留；这些是 docs-only/CI 事实，不等于 Auth/DB/运行时通过。G2-A1 技术阶段尚未开始；最小资源存在性/基础预检已完成；完整 resource/cost/secret Gate 仍关闭；P2–P8 保持未打开。G1 的本地/远端证据仍按对应历史记录保存；Auth spike 实施、Preview/生产连接仍未开始。
- 本文不把本地证据写成 production；生产、数据库、支付和隐私风险在相应阶段必须专项审查。

### 2026-08-27｜当前状态纠正（历史快照，已由 2026-08-28 current correction 取代）

本条记录当时取代本文件顶部及本节此前将 G1 写成“执行中/待 G1.2b/G1 Exit”、将 G2-A0 写成未打开的当前态表述；带日期的阶段记录和证据仍保留为历史快照。该历史快照中的 G1 Exit=GO、G2-A0 docs-only 执行中及远端 reconciliation 尚未执行，均已由 2026-08-28 current correction 取代；A0–A6 与 P0–P8 的正式映射尚未完成，不得从 A1 自动打开 P2/P3+。

### 2026-08-27｜G2-A0 Exit GO 当前态补充（历史快照，已由 2026-08-28 current correction 取代）

本条记录当时覆盖上一条“Exit 待完成”的当前态摘要，不删除其历史快照：G2-A0 Exit 已由 Owner 以验收 ref=`140ea15d9c3f178a326709d35ad1750a156df0d1` 于 2026-08-27 明确 GO；该条当时记录的远端 reconciliation 尚未执行及 closeout head 待复审，已由 2026-08-28 current correction 取代。G2-A1、P2–P8 未打开，资源/费用/secret/Auth/DB/Storage/Production Gate 继续关闭。

### 2026-08-28｜当前状态纠正

G2-A0 Exit GO（验收 ref=`140ea15d9c3f178a326709d35ad1750a156df0d1`），且远端 docs-only reconciliation 已完成：PR #7 merge=`fd9b712c7b07bf34399f9838eebb75846425c1d1`，parents=`7ea1e45ad22ab29105910665baf4bbd7212241c5` + `1433e7c7c141df0f5498fff7cd645a8d5c92340c`，main Actions run=`33122238997`/job=`98691703085` 全绿，GitHub deployments=0，来源分支保留；这些事实不等于 Auth/DB/运行时通过。G2-A1 技术阶段尚未开始；最小资源存在性/基础预检已完成；完整 resource/cost/secret Gate 仍关闭；P2–P8 未打开。详见 [G2-A1 entry preparation 证据](./evidence/G2-A1/2026-08-28-entry-preparation/README.md)。
