# Rebuy 项目状态与阶段台账

文档状态：唯一当前状态源；状态更新必须追加事实或带日期纠正  
台账日期：2026-08-28（Europe/Rome）
维护边界：本台账记录阶段状态和下一动作，不把本地证据写成 Staging、受控生产或生产验收。

## 1. 使用规则

执行路线见 [全局执行总计划](./14-全局执行总计划.md)，阶段事实见 [阶段记录索引](./stages/README.md)，README 只做导航。任何其他文档中的“当前状态”都是说明性摘要；需要更新状态时先改本台账，再按需要同步 README/阶段记录，避免形成第二份每日状态源。

阶段状态只能使用以下枚举：

| 状态 | 含义 |
|---|---|
| 未开始 | 已有规划或依赖，但本阶段尚未执行 |
| 准备中 | 前置条件/环境/人员正在准备，尚未进入阶段性实现或验证 |
| 执行中 | 本阶段已有授权工作正在进行，阶段 Exit/全部 Gate 尚未完成 |
| 待 Owner 验收 | 阶段产物和最小证据已形成，等待 Owner 全部验收；不等于已通过 |
| 已通过 | Owner Gate 已明确通过，并有可追溯决定记录 |
| 已阻塞 | 依赖或明确风险使阶段不能继续，已记录阻塞原因和解除条件 |
| 已取消 | Owner 明确取消，不再按原路线继续 |

证据级别只能使用以下枚举：

| 证据级别 | 能证明什么 | 不能证明什么 |
|---|---|---|
| 规划 | 范围、合同、依赖、假设或设计意图 | 代码、环境、真实数据或生产能力 |
| 本地静态 | 文件、类型、lint、构建、链接或静态检查在本地成立 | 真实用户、权限、网络、部署或生产 |
| 本地交互 | 本地原型/应用在指定视口和合成数据下可见、可点击 | 真实认证、数据库、RLS、订单写入或生产验收 |
| 独立测试 | 隔离测试环境中的受控功能和负向证据 | Staging、受控生产或生产数据 |
| Staging | 经隔离的预发布环境验证 | 生产流量、真实业务批准或长期运维 |
| 受控生产 | 有明确范围、监控、回滚和书面批准的生产试运营 | 全量生产完成或风险自动解除 |
| 生产 | 生产系统在对应 Owner/专项门下正式验收 | 不代表其他阶段自动通过 |

## 2. 当前阶段台账

| 阶段 | 状态 | 证据级别 | 依赖 | 最近更新 | Owner 决策 | 记录链接 | 下一动作 |
|---|---|---|---|---|---|---|---|
| P0 文档基线 | 已通过 | 本地静态 | 当前范围、01/02 设计基线 | 2026-08-25 | 文档基线可作为后续执行入口；不等于产品或生产通过 | [05](./05-V1实施路线与验证计划.md) | 按 GOV-1 维护唯一状态与阶段记录 |
| GOV-1 治理与状态台账 | 已通过 | 本地静态 | P0 | 2026-08-25 | Owner 于 18:58:57 CEST 原话“批准”；仅批准 GOV-1 治理体系并正式打开 G0/P1 Owner 视觉验收，不等于 G0/G1 或生产通过 | [GOV-1 阶段记录](./stages/GOV-1-治理文档与状态台账.md) | 按 [G0/P1 Owner 视觉验收清单](./stages/G0-P1-Owner视觉验收清单.md) 执行；G0 明确通过前不打开 G1 |
| G0/P1 视觉原型与 UI 冻结 | 已通过并冻结 | 本地交互 | GOV-1、01/02/06/12/13、合成数据原型 | 2026-08-25 23:06:57 CEST（Owner 分类目录 IA 决定） | Owner 原话 `分类目录IA通过，G0重新冻结，未覆盖项进入后续专项，恢复G1授权`；点击搜索已通过，直接 Enter 键盘提交未验证成功，转入后续键盘/无障碍专项，不阻塞本次冻结或 G1 Entry | [G0 阶段记录](./stages/G0-P1-视觉验收与UI冻结.md)；[Owner 验收清单](./stages/G0-P1-Owner视觉验收清单.md)；[IA 复验证据](./evidence/G0-P1/2026-08-25-category-directory-ia/README.md)；[统一选择器证据](./evidence/G0-P1/2026-08-25-unified-select/README.md)；[全流程验收证据](./evidence/G0-P1/2026-08-25-full-experience-acceptance/README.md) | G0 继续冻结；G1 已完成且 Exit=GO；G2-A0 Exit GO，远端 docs-only reconciliation 已完成；G2-A1 执行中（B1 配置/能力只读预检已完成），下一步形成 B2 专项风险 Gate 草案并等待独立复审与 Owner/主代理 Gate；完整 resource/cost/secret Gate 仍关闭 |
| G1 工程底座与环境隔离 | 已通过 | 本地静态 + 本地等价 + 远端 Actions + provider 只读 + Owner Gate | G0/P1 已通过并冻结、14 工程合同 | 2026-08-27 | Owner 已签署 G1-19，G1 Exit=GO，日期=`2026-08-27`，验收 ref=`d51f1c7cb47e2fe2932b29bd39420f5d092a8160`；PR #5 merge 双 parent 为 `af6d7419956ce6640c0b4af5df4db0369e793f77` + `824dd27f37792b3f487ec7a9ab21270b4b97fb84`；main Actions run=`33089108238` / job=`98576781415` 四步 success；bad PR #4、good/bad Preview、Production asset/aliases fingerprint、Protection、provider rootDirectory 与 Preview env 不变量保持 | [G1 Owner 验收清单](./stages/G1-Owner验收清单.md)；[G1 final closeout evidence](./evidence/G1/2026-08-27-g1-final-closeout/README.md)；[G1.3-2](./evidence/G1/2026-08-27-g1-3-2-preview-execution-review/README.md)；[G1.3-3](./evidence/G1/2026-08-27-g1-3-3-good200-local-candidate/README.md) | G1 已关闭；G2-A0 Exit GO，远端 docs-only reconciliation 已完成；G2-A1 执行中（B1 配置/能力只读预检已完成），下一步形成 B2 专项风险 Gate 草案并等待独立复审与 Owner/主代理 Gate；完整 resource/cost/secret Gate 仍关闭。既有 branches/deployments 保留；禁止 squash、rebase、force/direct push、删除分支/deployment、Production/promote/deploy/Supabase/Auth/DB |
| G2-A0 账号安全合同与威胁模型 | 已通过 | 本地静态 + 远端 merge/Actions + 独立复审 | G1 Exit Owner Gate、07/08/09、G2-A0 Entry 授权 | 2026-08-28 | G2-A0 Exit GO；验收 ref=`140ea15d9c3f178a326709d35ad1750a156df0d1`；Owner 已采纳七项政策；PR #7 merge=`fd9b712c7b07bf34399f9838eebb75846425c1d1`，main Actions run=`33122238997`/job=`98691703085` 全绿；GitHub deployments=`0`，来源分支保留 | [G2-A0 阶段记录](./stages/G2-A0-账号安全合同与威胁模型验收.md)；[G2-A0 Owner 清单](./stages/G2-A0-Owner验收清单.md)；[09](./09-A0-账号架构ADR与威胁模型.md)；[G2-A1 Entry preparation 证据](./evidence/G2-A1/2026-08-28-entry-preparation/README.md) | G2-A0 已关闭；G2-A1 当前执行中（B1 配置/能力只读预检已完成），完整 resource/cost/secret Gate 仍关闭；下一步形成 B2 专项风险 Gate 草案并等待独立复审与 Owner/主代理 Gate，不自动打开后续 Gate |
| G2-A1 独立 Auth spike | 执行中（B1 连接及配置/能力只读预检已完成） | 本地静态 + 本地交互 + 外部 Auth 管理面只读核验 | G2-A0 Owner Gate、10/11 独立测试边界、Entry preparation、B1 风险 Gate | 2026-08-28 | B1 最小连接与 Auth 配置/能力只读预检已完成；本批 findings 由 `7952d16` 修复；独立定向复审对该 exact head 给出 REVIEW GO，无未关闭 P0/P1/P2；该 GO 只关闭 B1 capability preflight 审查，不打开 B2 或任何 Auth/DB/Storage/OAuth/SMTP/费用/部署/Production Gate；不预写 push/PR/Actions/merge；完整 Auth spike、真实登录和真实数据仍未开始；B2 风险 Gate 草案待独立安全复审与 Owner/主代理 Gate | [G2-A1 准备与资源门禁](./stages/G2-A1-Auth-Spike准备与资源门禁.md)；[B1 配置/能力预检证据](./evidence/G2-A1/2026-08-28-b1-capability-preflight/README.md)；[B1 风险 Gate 证据](./evidence/G2-A1/2026-08-28-b1-risk-gate/README.md)；[G2-A1 Entry preparation 证据](./evidence/G2-A1/2026-08-28-entry-preparation/README.md)；[10](./10-A1-Auth-Spike执行合同.md) | 下一步形成 B2 专项风险 Gate 草案并等待独立安全复审与 Owner/主代理 Gate；B2 及完整 resource/cost/secret/Auth/DB/Storage/OAuth/SMTP/真实 PII/部署/Production Gate 继续 CLOSED，service_role/secret key/db password 禁止 |
| P2 登录/组织/权限纵切 | 未开始 | 规划 | G0、G1、G2-A0、G2-A1、03/04 | 2026-08-25 | 当前未打开；不得把 P1 身份样本当作权限实现 | [14 P2–P8 阶段合同](./14-全局执行总计划.md) | 等 G0、G1、G2-A0、G2-A1 全部 Gate 后重新确认数据/RLS/授权范围 |
| P3 商家入驻与平台审核 | 未开始 | 规划 | P2、审核资料/SLA 决策 | 2026-08-25 | 当前未打开 | [14 P2–P8 阶段合同](./14-全局执行总计划.md) | 等 P2 通过后形成资料、隐私和审核 Owner 决定 |
| P4 商品/定价/库存 | 未开始 | 规划 | P2、P3、价格/库存决策 | 2026-08-25 | 当前未打开；P1 价格/库存仅为合成演示 | [14 P2–P8 阶段合同](./14-全局执行总计划.md) | 等权限和有效商家条件通过后设计 schema/RLS/并发验证 |
| P5 浏览/购物车/订单 | 未开始 | 规划 | P2、P4、订单/地址决策 | 2026-08-25 | 当前未打开；本地购物车/订单不产生持久化 | [14 P2–P8 阶段合同](./14-全局执行总计划.md) | 等真实价格/库存边界和幂等方案通过后执行 |
| P6 商家后台闭环 | 未开始 | 规划 | P2–P5、商家职责/售后决策 | 2026-08-25 | 仅有设计映射；未授权 Merchant Shell 或后台实现 | [13](./13-买家端与商家后台页面及组件映射.md)、[14 P2–P8 阶段合同](./14-全局执行总计划.md) | 依赖前置业务和权限审查；另开后台 Owner Gate |
| P7 受控试运营 | 未开始 | 规划 | P2–P6、生产/备份/监控/隐私 | 2026-08-25 | 当前未打开；不使用真实 PII 或生产写入 | [14 P2–P8 阶段合同](./14-全局执行总计划.md) | 只有全部前置专项通过后准备发布候选与回滚演练 |
| P8 后期支付/物流/税务 | 未开始 | 规划 | P7、商业/法律/税务/供应商决策 | 2026-08-25 | V1 当前不包含；不得越级实现 | [14 P2–P8 阶段合同](./14-全局执行总计划.md) | 另行形成商业与专项审查合同后再决定 |

## 3. 已确认决策与当前边界

| 日期 | 决策/事实 | 影响 |
|---|---|---|
| 2026-08-25 | Owner 已批准统一 UI 文档并选定配色 D「循环翡翠青」，已同步既有本地 P1 原型 | G0/P1 使用 D token；不代表后台、后端或生产授权 |
| 2026-08-25 | 买家五项导航、多商家购物车/订单分组、批发身份自动定价、二手关键事实和 zh/it/en 是已批准业务事实 | P1 视觉验收和后续 P2–P5 必须保持；不能改成手动批发模式 |
| 2026-08-25 | 本批治理文档已写入，主代理审查和两轮纠正已完成；GOV-1 已于 18:58:57 CEST 通过 | GOV-1 通过只维持治理体系，不替代 G0/P1 当前修订或后续工程 Gate |
| 2026-08-25 | G1 工程底座未开始（Entry 恢复前的只读事实/历史）；根目录没有可用 Git 元数据，版本基线待建立/可能阻塞；实际 Node `v20.20.2`，项目要求 `22.x`，pnpm `10.33.3` 与声明一致 | 这是 Entry 恢复前的历史记录；不捏造 commit。当前 G1 已获 Entry 恢复授权，但仍须先完成 Node 20→22 对齐，再建立版本、Node 22/包管理器、CI 和四环境隔离；typecheck/lint 的通过及 engine warning 不等于 G1 已满足 |
| 2026-08-25 23:06:57 CEST | Owner 原话 `分类目录IA通过，G0重新冻结，未覆盖项进入后续专项，恢复G1授权` | 当前 G0/P1 已通过并冻结、checkpoint 16 已通过；G1 为“准备中（Entry 已恢复授权）”，先做 G1.1 Git 根选择与 Node 22/pnpm 10.33.3 基线，不表示工程底座已完成或已打开 G2-A0 |
| 2026-08-25 | Owner 原话 `继续下一步`；按已批准范围采用项目根为唯一 Git 根、`prototype/` 为 app 目录并复用 Node 22.12.0/pnpm 10.33.3 | G1.1 Git/main/repo-local identity/初始 SHA 已建立；offline frozen install 因 `@swc/helpers@0.5.23` 缓存缺失阻塞，G1.1 尚未完成；G1.2/G1.3 未开始，不打开 G2-A0 |
| 2026-08-25 | 隔离副本完成 Node22 frozen install、typecheck、lint、build 补验；验证源为初始提交 `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb` | G1.1 更新为已完成；G1 仍执行中，G1.2/G1.3 未开始；G1 Exit 未通过，G2-A0 不打开；无 CI/Preview/远端/Supabase/生产连接 |
| 2026-08-26 | G1.2 最小 CI preflight 完成：拆分 G1.2a 本地 workflow 候选/等价验证与 G1.2b 未来远端真实 run；推荐 GitHub Actions、ubuntu-24.04、Node 22.12.0、pnpm 10.33.3/Corepack、prototype working-directory、只读权限和无 secrets/health/artifact/deploy | G1 当前为“执行中（G1.1 已完成，G1.2 待 Owner Gate）”；未创建 `.github/`、remote、CI run、Preview、Supabase 或生产；等待 Owner 明确授权 G1.2a |
| 2026-08-26 | 供应链预检核验官方不可变 Action 候选：`actions/checkout` v7.0.1 → `3d3c42e5aac5ba805825da76410c181273ba90b1`；`actions/setup-node` v7.0.0 → `820762786026740c76f36085b0efc47a31fe5020`；均保留官方 release → commit 链路 | 仅作为 G1.2a 文档候选；G1.2a 尚未获批，未创建 workflow/remote/CI；实施当天仍须按 secure-use 指南复核，漂移时先更新证据和 Owner 决策，不改变当前 Gate |
| 2026-08-26（初版历史快照） | Owner 批准 G1.2a：采用 GitHub Actions 候选，在本地创建只读最小 workflow 并做等价验证；workflow commit `a388348b81300ca00f669d0bd62b0748b9f191a5`，隔离 Node `v22.12.0`/Corepack `0.29.4`/pnpm `10.33.3` 下 frozen install、typecheck、lint、build 均退出 0 | 当时记录为 G1.2a 本地静态/等价证据已完成；G1.2b 尚未获批，无 remote、push、远端 CI、Preview、Supabase 或生产连接；后续 Corepack 证据已由主审纠正，详见[G1.2a 证据](./evidence/G1/2026-08-26-g1-2a-local-workflow/README.md) |
| 2026-08-26 | 主审修正 G1.2a Corepack 证据：将旧 Corepack `0.29.4` warmed-cache 结果降级为历史诊断；当前 workflow `b0681d585cabe2f5f293779fc3627e2782be9fa2` 通过 npm exact bootstrap Corepack `0.34.6`，fresh 等价验证中 pnpm `10.33.3`、frozen install、typecheck、lint、build 均退出 0 | G1.2a 当前本地 workflow/等价验证完成；G1.2b 仍待 Owner Gate，非远端 CI 通过；无 remote、push、Preview、Supabase 或生产连接；G1 Exit 未通过，G2-A0 不打开 |
| 2026-08-25 | G2-A0/G2-A1 只有 A0/A1 合同、本地连接骨架和依赖检查，Auth spike 实施未开始；P2 未打开 | G2-A0 需独立安全审查；不连接 Auth、Staging 或生产，不创建真实身份/组织/权限 |
| 2026-08-25 18:58:57 CEST | Owner 原话“批准”，上下文为批准 GOV-1 治理体系并进入 G0/P1 Owner 视觉验收 | GOV-1 更新为已通过；G0/P1 正式打开但仍待 Owner 验收；不打开 G1 或任何代码/数据库/Auth/生产实施 |
| 2026-08-25（20:14:15 CEST） | Owner 原话 `G0视觉通过并冻结，授权进入G1` | 当时关闭 G0/P1 并记录 G1 Entry 授权；该决定保留为历史，不代表后续修订后的当前状态 |
| 2026-08-25（20:14:15 之后，精确时间未提供） | Owner 原话 `整个项目当中不要出现类似这样的目录，我要的是移动端app的那种体验`；分类页证据为 `REBUY / CATALOG` | G0/P1 冻结因新修订请求重新打开；修订后进入“待 Owner 验收”，G1 仍“未开始”，历史 Entry 授权暂停执行 |
| 2026-08-25 | Owner 原话 `这个点击打开的选项卡不统一 帮我为项目制作统一风格的`；分类页排序展开显示浏览器原生蓝色菜单 | G0/P1 暂记为“执行中（统一选择器修订）”；只修订本地 UI 选择器，不改变业务筛选、分类、五项导航或后端边界；G1 继续“未开始/暂停” |
| 2026-08-25 | 统一选择器代码与实时浏览器验收完成：499/390/1440 视口的菜单边界、明暗主题、键盘循环、Tab/Shift+Tab 焦点和语言同步均有本地交互证据；原生 `<select>` 扫描为 0 | G0 本轮统一选择器修订待 Owner 验收；G1 继续未开始/暂停；本地证据不等于 UI 冻结或生产验收 |
| 2026-08-25 | Owner 原话 `统一选择器通过，继续G0总体验收`；checkpoint 15 仅统一选择器通过，主代理继续完成 G0 全流程验收 | G0 总体仍待 Owner 验收；全流程机器证据和隔离 build 完成；G1 继续未开始/暂停，不因 checkpoint 15 打开 |
| 2026-08-25（具体时刻未提供） | 保留历史 Owner 原话 `G0总体验收通过并冻结，未覆盖项进入后续专项，重新授权进入G1`；随后 Owner 新要求 `然后网站整体的目录结构应该是跟淘宝类似，首页显示推荐内容，然后分类展示各个种类的列表，点进去后才是商品` | 后一条是新的核心 IA 修订，不抹除前一条；G0/P1 重新打开并进入“执行中（分类目录信息架构修订）”，G1 Entry 历史授权暂停，不开始工程 |
| 2026-08-25 | 主代理完成分类目录 IA 本地浏览器复验；新增 IA evidence manifest | G0/P1 更新为“待 Owner 验收（分类目录 IA 修订及主代理本地浏览器复验完成）”；checkpoint 16 机器证据完成但 Owner 决定仍待验收；G1 继续未开始/暂停 |

### 2026-08-25 23:06:57 CEST｜分类目录 IA 通过，G0 重新冻结并恢复 G1 Entry

- Owner 最新正式决定原话：`分类目录IA通过，G0重新冻结，未覆盖项进入后续专项，恢复G1授权`。
- 当前状态：G0/P1 更新为“已通过并冻结”；checkpoint 16 更新为“通过”。点击搜索已通过；直接 Enter 键盘提交未验证成功，转入后续键盘/无障碍专项，不阻塞本次 G0 冻结或 G1 Entry。
- G1 更新为“准备中（Entry 已恢复授权）”。本条只打开 G1 Entry 准备，不代表工程底座已完成；尚未初始化 Git、切换 Node、创建 CI/Preview、连接远端/Supabase/生产。
- 只读基线证据：[G1 Entry 基线](./evidence/G1/2026-08-25-entry-baseline/README.md)。项目根与 `prototype/` 均无 `.git`；当前 Node `v20.20.2`、pnpm `10.33.3`、`prototype/.node-version=22`、package engines `22.x/10.33.3`、`packageManager=pnpm@10.33.3`、lockfileVersion `9.0`；未发现 CI/部署配置；只观察 `.env.example` 两个变量名，未读值。
- G1.1 推荐项目根作为唯一 Git 根、`prototype/` 作为 app 目录，但仍需 Owner 作出物质选择；确认后先建立本地 ref 与 Node 22/pnpm 10.33.3 基线，再另行决定远端/Preview。不得凭空写 commit ref。
- 范围边界：不修改 prototype、代码、依赖、服务器、数据库、Auth、部署、外部服务或生产数据；G1 Exit Gate 未通过，G2-A0 仍未开始。

### 2026-08-25｜G1.1 本地 Git 基线建立，Node22 离线依赖验证阻塞

- Owner 原话：`继续下一步`。按已批准范围使用项目根作为唯一 Git 根、`prototype/` 作为 app 目录；不创建 remote、CI、Preview、Supabase 或 production 连接。
- 初始基线：分支 `main`；repo-local identity `Hexiang Huang`（邮箱已脱敏）；初始提交 `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb`；预定稳定 tag `g1.1-local-baseline-2026-08-25`。
- 暂存集审查：79 个文件；仅允许 `prototype/.env.example`；未跟踪 `.env`、node_modules、`.next`、`.pnpm-store`、`.vercel`、`.supabase`、`*.tsbuildinfo` 或明显敏感值；无 remote。
- 工具链：明确 PATH 下 Node `v22.12.0`、pnpm `10.33.3`。首次 offline install 因无 TTY 拒绝清理 modules；`CI=true` 重试后因缓存缺少 `@swc/helpers@0.5.23` 退出 `ERR_PNPM_NO_OFFLINE_TARBALL`。未联网、未改 lockfile；install 失败后停止依赖验证。
- typecheck/lint：仅在第一次非交互 install 被拒绝、既有 node_modules 尚未重建前通过；不能当作失败 install 后的当前完整通过。此前相同源码/config/lockfile 的隔离 Node22 build 证据按条件复用，不重复声称本批 build。
- 当时状态（历史快照）：G1 为“执行中（G1.1 本地 Git 基线已建立，Node22 离线依赖验证阻塞）”；G1.1 部分完成但未完成，G1.2/G1.3 未开始；G1 Exit 未通过，G2-A0 不打开。完整证据见 [G1.1 本地基线](./evidence/G1/2026-08-25-g1-1-local-baseline/README.md)。

## 4. 记录与纠错日志

### 2026-08-25｜初始台账

- 建立 14 的稳定路线、15 的唯一状态源、阶段模板、阶段索引和 G0/P1 事实记录。
- 采用本文件定义的状态和证据枚举；未记录密码、token、客户 PII、真实商家资料、原始敏感附件或生产凭据。
- 初始批次曾将 `GOV-1` 标为“执行中”；本次独立阶段记录和主代理审查完成后，当前状态纠正为“待 Owner 验收”。`G0/P1` 仍为“待 Owner 验收”，G1、G2-A0、G2-A1、P2–P8 仍为“未开始”；以上状态不表示 Owner 已通过。

### 2026-08-25｜GOV-1 状态与独立记录纠正

- 已创建 [GOV-1 独立阶段记录](./stages/GOV-1-治理文档与状态台账.md)，并将台账状态从初始“执行中”纠正为“待 Owner 验收”。
- 主代理已完成内容审查并要求的两轮路线/一致性纠正已落实；Owner 尚未确认治理体系。
- 下一动作是 Owner 批准/修订 GOV-1，随后按路线完成 G0/P1 视觉验收；不改变 G0 或后续阶段状态。

### 2026-08-25 18:58:57 CEST｜Owner 批准 GOV-1，打开 G0/P1 Owner 验收

- Owner 原话为 `批准`，上下文是主代理请求“批准 GOV-1 治理体系，进入 G0/P1 Owner 视觉验收”。
- 当前状态：GOV-1 已通过；G0/P1 仍为“待 Owner 验收”，下一动作改为按 [Owner 视觉验收清单](./stages/G0-P1-Owner视觉验收清单.md) 执行。
- 范围边界：不把该决定解释为 G0/P1 UI 冻结/通过，不打开 G1，不授权代码之外的数据库、Auth、部署、生产、支付或隐私实施；G1、G2-A0、G2-A1、P2–P8 仍未开始。

### 2026-08-25｜G1 预编制合同完成但阶段未打开

- 事实：已创建 [G1 工程底座与环境隔离预编制合同](./stages/G1-工程底座与环境隔离.md)，并记录根目录无 `.git`、Node `v20.20.2` 与项目 Node `22.x` 要求不一致、pnpm `10.33.3`、`.node-version=22`、lockfile 和变量名级证据。
- 影响：合同明确 G0/P1 必须先取得 Owner“通过并冻结”决定；若为“修订”，必须修复、复验并取得新的通过决定；G1 Entry Gate 与 Exit Gate 均未通过。
- 处理：本批仅完成 Markdown 预编制和只读核实，没有初始化 Git、修改工程、创建 CI、安装依赖、创建环境、连接外部服务或改变数据库/Auth/部署。
- 状态：G1 仍为“未开始”，G0/P1 仍为“待 Owner 验收”；本条不构成 G1 开门或任何工程授权。

### 2026-08-25｜Owner 修订请求重新打开 G0/P1，暂停 G1 进入

- Owner 原话：`整个项目当中不要出现类似这样的目录，我要的是移动端app的那种体验`；上下文为分类页出现 `REBUY / CATALOG` 目录/路径式 eyebrow。
- 历史决定保留：Owner 于 20:14:15 CEST 原话 `G0视觉通过并冻结，授权进入G1`，当时 G0/P1 已记录通过并冻结、G1 Entry 已获授权。
- 当前纠正：新修订请求使 G0/P1 冻结重新打开；本批标题层级修订已完成并通过源码/typecheck/lint 机器检查，15 台账当前状态改为“待 Owner 验收”。G1 仍为“未开始”，Entry 进入暂停，必须等待新的 G0“通过并冻结”决定，不能继续工程工作。
- 本批边界：代码只修改买家端标题层级与相关间距；文档仅同步状态和证据边界；不修改业务数据、Git、CI、环境、数据库、Auth、部署、生产或外部服务；保留前序机器证据、Owner 操作路径和 20:14:15 决定历史。

### 2026-08-25｜移动 App 标题层级修订完成，提交 G0 复验

- 目标：响应 Owner 对分类页 `REBUY / CATALOG` 目录/路径式表达的反馈，使买家端主页面直接呈现本地化标题和原生移动 App 分区。
- 实际改动：`prototype/components/PrototypeApp.tsx` 删除买家路径式 eyebrow 与首页、详情、结账、订单详情、个人页的纯装饰 `01`/`02`/`03`；保留详情标题/数量、购物车总计、订单批次/订单号等业务语义。`prototype/app/globals.css` 合并标题间距规则，`.page-section` 不再叠加 32px 假间距；配色 D、五项导航、分类和业务事实不变。
- 证据：[移动 App 标题层级 manifest](./evidence/G0-P1/2026-08-25-mobile-app-titles/README.md)记录本批源码扫描、Node 22 typecheck/lint，以及主代理已完成的本地浏览器交互复验；未保存新的截图文件，不能由本条预填 Owner 通过。
- 主代理交互结果：390×844 中文浅色五主页面均 `pathLabels=[]`、`numericSectionLabels=[]`、`bottomNav=5`、`overflowX=false`；it/en 分类 h1 与 `document.lang` 分别为 `Categorie e ricerca`/`it`、`Categories and search`/`en`；1440×900 中文分类与购物车均无横向溢出，分类显示桌面 sidebar grid、购物车为 2 栏；console/page error/warn 均为 `[]`。截图证据仍待后续补存或 Owner 当前可见预览判断。
- 验证：源码 `rg` 门禁无 `Rebuy /`、`REBUY /` 或数字装饰 eyebrow；Node 22.12.0 下 `pnpm typecheck`、`pnpm lint` 通过；未运行 build，未做独立审查、哈希或生产验证。
- 当前状态：G0/P1 为“待 Owner 验收”，冻结尚未重新通过；G1 为“未开始”，历史 Entry 授权暂停。下一动作是主代理/Owner 复验移动 App 标题层级与全流程，再决定通过、修订或暂停。

### 2026-08-25｜统一选择器修订启动

- Owner 原话：`这个点击打开的选项卡不统一 帮我为项目制作统一风格的`；可复核证据为分类页排序打开后出现浏览器原生蓝色菜单。
- 当前状态：G0/P1 暂记为“执行中（统一选择器修订）”；历史 G0 冻结、标题层级修订和 G1 Entry 授权均保留为历史，不升级为当前通过或工程授权。
- 本批范围：新增共享 `SelectMenu<T extends string>`，替换分类排序和顶部语言两个原生选择器；统一循环翡翠青 token、明暗主题、焦点、键盘、点击外部关闭和 44px 触控。真实商品筛选、分类、五项导航、排序值、语言同步和业务数据不变。
- 排除：不修改商品/订单业务逻辑、后台、数据库、Auth、部署、Git、CI、外部服务或生产数据；浏览器视觉验收由主代理另行执行。
- 下一动作：完成源码门禁、typecheck/lint 后由主代理复验两处 SelectMenu 的打开、键盘、焦点、明暗主题和移动视口边界；G1 继续“未开始/暂停”。

### 2026-08-25｜统一选择器实时浏览器验收完成，待 Owner Gate

- Owner 原话：`这个点击打开的选项卡不统一 帮我为项目制作统一风格的`；本条是该修订请求的完成证据，原启动记录保留。
- 证据：[统一选择器实时验收 manifest](./evidence/G0-P1/2026-08-25-unified-select/README.md)。本轮没有落盘 PNG，不把实时 DOM/CSS 检查写成截图证据。
- 实际结果：499×862 浅色排序菜单为 `212×150`、每项 44px、圆角 14px、视口内且无系统蓝色；390×844 深色菜单视口内且 `scrollWidth=clientWidth=390`；1440×900 语言菜单右对齐不越界，zh-CN/it/en 键盘切换与标题同步完成；排序选择、Sheet 联动、Escape、Tab/Shift+Tab 均按预期移焦，不落 BODY；控制台无 warning/error。
- 验证：`prototype/ pnpm typecheck`、`prototype/ pnpm lint` 通过；原生 `<select>` 扫描为 0。未运行 build/E2E，未做 hash（无生成物/落盘截图），未启动额外独立审查代理；本批为普通共享 UI 调整，主代理已完成源码与实时浏览器综合复验。
- 当前状态：G0/P1 为“待 Owner 验收”，准确描述为“G0 本轮统一选择器修订待 Owner 验收”；G1 仍“未开始/暂停”。20:14:15 的历史冻结与 Entry 授权保留为历史，不恢复为当前工程授权。
- 边界/回退：只涉及本地 UI 选择器与证据记录，不改变业务、后端、Auth、数据库、部署或生产；若 Owner 修订，可局部回退 SelectMenu 及调用点并追加新证据，不覆盖本条。

### 2026-08-25｜Owner 通过统一选择器，继续 G0 总体验收

- Owner 原话：`统一选择器通过，继续G0总体验收`（2026-08-25；具体时刻未提供）。严格解释：仅将 Owner 清单 checkpoint 15 标为“通过”，不等于 G0/P1 总体通过、UI 冻结、G1 Entry 或生产授权。
- 主代理实时证据：[G0/P1 全流程验收 manifest](./evidence/G0-P1/2026-08-25-full-experience-acceptance/README.md)。覆盖 390/430/768/1024/1440、首页、分类/搜索/筛选、普通/二手详情、购物车/结算、订单/我的、登录、明暗主题、zh/it/en、零售/批发样本和响应式边界；console/page warning/error=0。
- 关键边界：320 精确视口未覆盖（浏览器实际 clamp 到 innerWidth=413）；全页 Tab 序列本轮复用此前定向键盘证据；200% 浏览器缩放、系统 reduced-motion=true、完整读屏/WCAG 未覆盖。主代理未提交演示订单、真实认证或外部请求。
- 隔离构建：最终 `/tmp/rebuy-g0-build.RKda6l/prototype` 使用 Node `v22.12.0`、pnpm `10.33.3`、Next `16.3.2` 完成 `pnpm build`，退出状态 0；外部 symlink/`cp -a` 失败尝试已记录于全流程 manifest。未停止/替换服务器；执行代理隔离 shell 的 curl 为 `000`，不作用户侧预览通过或失败证据，主代理 in-app browser 构建前后持续可交互。
- 当前状态：G0/P1 仍为“待 Owner 验收（总体验收机器证据完成，等待最终冻结/修订决定）”；G1 仍“未开始/暂停”。下一动作是 Owner 对全流程结果作出通过、修订或暂停决定。

### 2026-08-25｜分类目录信息架构修订启动

- 历史决定保留：Owner 曾原话 `G0总体验收通过并冻结，未覆盖项进入后续专项，重新授权进入G1`；本条不删除或重解释该当时的冻结与 G1 Entry 记录。
- 新 Owner 要求原话：`然后网站整体的目录结构应该是跟淘宝类似，首页显示推荐内容，然后分类展示各个种类的列表，点进去后才是商品`。这里仅借鉴层级关系，不复制淘宝品牌、配色、拥挤营销或视觉。
- 当前状态纠正：G0/P1 重新打开并改为“执行中（分类目录信息架构修订）”；G1 仍“未开始/暂停”，历史 Entry 授权不能作为当前工程授权。
- 本批实现：复用 `categoryItems`、`productMatchesCategory`、`ProductCard`、`FilterSheet`、`AppShell` 和现有 token；扩展 `catalogMode` 为 directory/results，增加本地化目录标题/返回文案和电脑品类。分类根页只渲染品类入口，不渲染商品卡、结果计数、筛选或排序；点击品类进入匹配商品列表并显示结果数/筛选/排序/返回全部分类；搜索直接进入结果；详情返回继续沿用来源列表状态。
- 验证：Node 22.12.0 下 `prototype/pnpm typecheck`、`prototype/pnpm lint` 通过；源码目录根页门禁确认无 `ProductCard`、`FilterSheet`、结果计数或排序结构，并确认无 `REBUY /`/装饰数字；本轮尚未由主代理完成浏览器复验，不写成 Owner 验收或 UI 冻结。
- 边界/回退：仅改本地买家 UI、数据文案和设计记录；不改后端、API、Auth、数据库、部署、CI、生产或商家后台。若浏览器复验发现回归，局部回退 `catalogMode`/目录呈现和对应样式，并追加新记录；G1 继续暂停。

### 2026-08-25｜分类目录 IA 浏览器复验完成，待 Owner 验收

- 证据：[分类目录 IA 复验证据 manifest](./evidence/G0-P1/2026-08-25-category-directory-ia/README.md)。主代理在 `http://127.0.0.1:3000/`、合成数据下完成 390/430/768/1024/1440 的目录、结果、返回、电脑侧栏、三语、浅深色和无溢出复验；本批未落盘截图。
- 实际结果：推荐先于快速分类；分类根为 4 品类且 ProductCard/article/筛选/排序/结果数均为 0；品类映射为手机配件 2、电子产品 3、二手交易 2、电脑与配件 1；详情返回、返回全部分类、桌面电脑快捷入口和点击搜索均通过；console/page warning/error 为 0。
- 搜索边界：390 输入 Voltix 时 form 158.84px、input 156.84px、左右 padding 48px、submit/clear 各 44px；点击 submit 进入 1 个 Voltix 结果，清除按钮正常。自动化 press("Enter") 与 CUA Enter 本轮均未产生结果跳转，不能写成键盘通过，留给后续键盘/无障碍专项和 Owner 手动复核。
- 验证：Node 22.12.0/pnpm 10.33.3 的 typecheck/lint、源码门禁、154 项 Markdown 链接/fragment、围栏和敏感模式扫描均通过；最后 CSS 修正复用前一状态 typecheck/lint 证据。未重跑 build/E2E/hash，未启动独立审查；原因和边界详见 manifest。
- 当前状态：G0/P1 改为“待 Owner 验收（分类目录 IA 修订及主代理本地浏览器复验完成）”，证据级别本地交互；checkpoint 16 机器证据完成但 Owner 决定仍待验收。G1 为“未开始/暂停”，历史 Entry 授权只有在新的 G0“通过并冻结”后才可恢复。

### 2026-08-25｜G1.1 Node22 可复现基线验证完成

- 验证源：初始源码提交 `0d6bb3f621019e3c8ad4b81e6614cd8c6bea5bcb`；在 `/private/tmp/rebuy-g1.ml6qyC/prototype` 的 git archive 隔离副本中运行，未复制工作区 `node_modules`，未修改当前 prototype 源码、package 或 lockfile。
- 实际结果：Node `v22.12.0`、pnpm `10.33.3`；`pnpm install --frozen-lockfile`、`pnpm typecheck`、`pnpm lint`、`pnpm build` 均退出 0。安装输出提示 pnpm 忽略 `unrs-resolver@1.12.2` build scripts；未执行 approve-builds。
- 影响：解除此前 `@swc/helpers@0.5.23` offline 缓存缺口和 Node22 脚本复验阻塞；G1.1 已完成。G1.2 CI、G1.3 四环境/Preview 回退未开始；G1 Exit 未通过，G2-A0 不打开。
- 边界：当前仍无 remote、CI、Preview、Staging、Production、Supabase/Auth、数据库、支付或真实 PII；隔离 build 未写入工作区。验证期间的 Node22 本地预览曾返回 HTTP 200，但执行代理 session/PID 仅属临时运行态，不属于 G1.1 完成或回退证据；后续以实时 HTTP/浏览器复核为准。
- 证据：[G1.1 基线证据](./evidence/G1/2026-08-25-g1-1-local-baseline/README.md)。

### 2026-08-26｜本地预览运行态运维交接

- 纠正：前一条 G1.1 记录中的具体 dev session 已降级为历史运行态事实；仅证明当时 Node22 本地预览 HTTP 200，不证明当前服务持续存在，也不构成 G1.1 完成或回退证据。
- 交接：后续主代理或 Owner 需要预览时，先实时检查 127.0.0.1:3000，再用浏览器复核；不得依赖旧 session/PID。该注记不改变 G1.1 tag、源码、隔离构建结论或阶段状态。

### 2026-08-26｜G1.2 最小 CI preflight 完成，待 Owner Gate（当时状态历史快照）

- 当时状态（历史快照）：G1 为“执行中（G1.1 已完成，G1.2 待 Owner Gate）”。G1.1 Node22/pnpm `10.33.3` 隔离 frozen install、typecheck、lint、build 已完成；G1.2 仅完成 docs-only preflight，不是 CI 创建或通过。
- 预检证据：[G1.2 preflight](./evidence/G1/2026-08-26-g1-2-ci-preflight/README.md)。G1.2a 是本地 workflow 候选/等价验证，G1.2b 是未来 remote 上的真实 CI run；当时无 remote、无 `.github/` workflow，因此不能把任何 G1.2a 规划写成 G1.2b 通过。
- 当时边界（历史快照）：未创建 `.github/`、remote、分支、tag、Preview、Staging、Production；未修改 prototype/package/lockfile；未读取 env/secrets/PII；未运行 workflow、远端 CI、health、artifact 或 deploy。
- 推荐 Owner 授权：`批准进入G1.2a：采用GitHub Actions候选，在本地创建只读最小CI工作流并做本地等价验证；暂不添加remote、不push、不运行远端CI、不部署Preview、不接Supabase或生产。`

### 2026-08-26｜Owner 批准 G1.2a，本地 workflow/等价验证完成（初版历史快照）

- Owner 已批准上述 G1.2a 授权语句；新增唯一本地 workflow `[prototype-quality.yml](../.github/workflows/prototype-quality.yml)`，commit `a388348b81300ca00f669d0bd62b0748b9f191a5`。
- 隔离副本使用 Node `v22.12.0`、Corepack `0.29.4`、pnpm `10.33.3`，按 install → typecheck → lint → build 顺序执行，四项均退出 0；保留 `unrs-resolver@1.12.2` ignored build-script warning，未执行 approve-builds/allowlist。
- 静态 YAML/策略、失败停止和基线回退检查完成；`prototype/**`、package、lockfile 无差异。无 remote、push、远端 CI、Preview、Supabase、数据库或生产连接。
- 当时状态：G1 为“执行中（G1.1 已完成，G1.2a 已完成，G1.2b 待 Owner Gate）”；G1 Exit 未通过，G2-A0/G2-A1/P2–P8 不打开。后续 Corepack 证据纠正见下条；证据：[G1.2a 接续记录](./evidence/G1/2026-08-26-g1-2a-local-workflow/README.md)。

### 2026-08-26｜主审修正 G1.2a Corepack exact bootstrap 并完成 fresh 等价重验

- 纠正：初版 workflow `a388348b81300ca00f669d0bd62b0748b9f191a5` 使用 Node22 bundled Corepack `0.29.4`；其 warmed-cache 成功与无缓存 `ENOTFOUND registry.npmjs.org` 均只保留为历史诊断。`ENOTFOUND` 发生在签名检查前，不能排除 npm 新签名 key/签名链或兼容失败，也不能证明 fresh runner 可安全获取 pnpm；参考 [Corepack issue #627](https://github.com/nodejs/corepack/issues/627)、[issue #612](https://github.com/nodejs/corepack/issues/612) 和 [官方 README](https://github.com/nodejs/corepack)。
- 当前实现：workflow 修正 commit `b0681d585cabe2f5f293779fc3627e2782be9fa2` 在 `${RUNNER_TEMP}/corepack` 临时 prefix 中通过 npm 精确安装 Corepack `0.34.6`，写入 `${GITHUB_PATH}` 后调用 pnpm `10.33.3`；不覆盖 runner Node 目录，不使用 `latest`、`0.35`、`pnpm/action-setup` 或完整性绕过。
- fresh 等价验证：Node `v22.12.0`、npm `10.9.0`、全新 npm cache/`COREPACK_HOME`/prefix；Corepack exact 安装退出 0，package version `0.34.6`、engines `^20.10.0 || ^22.11.0 || >=24.0.0`；Corepack `0.34.6` 与 pnpm `10.33.3` 解析退出 0；`pnpm install --frozen-lockfile`、`typecheck`、`lint`、`build` 均退出 0（351 packages，复用 351、下载 0），保留 `unrs-resolver@1.12.2` ignored build-script warning，未执行 approve-builds/allowlist。
- 当前状态：G1 为“执行中（G1.1 已完成，G1.2a 已完成，G1.2b 待 Owner Gate）”；本地 workflow/等价验证已完成，但不是远端 CI 通过；G1.2b、G1 Exit 未通过，G2-A0/G2-A1/P2–P8 不打开。npm registry 网络读取、签名链和未来 GitHub runner 行为仍属维护风险；失败时保留最小脱敏错误并回退 workflow commit，不静默换版本。

### 2026-08-26｜G1.2b-0 远端目标只读审计完成，G1.2b 待 Owner Gate

- 当前状态不变：G1 为“执行中（G1.1 已完成，G1.2a 已完成，G1.2b 待 Owner Gate）”；本批只读审计不等于远端 CI 运行或 G1.2b 通过，G1 Exit 未通过，G2-A0/G2-A1/P2–P8 不打开。
- 候选发现：公开账户仓库列表中 `kyox215/REBUY_SHARE` 的名称、description `意大利本地化交易app` 与 Rebuy 最匹配；已核验为 owner `kyox215`、public、default branch `main`、未归档、未禁用、非 fork，但仍需 Owner 确认 canonical repository。
- 远端只读结果：`main` tip 为 `366ad7f287a00f795c742d7f2df10a531fa42e7c`；公开 workflow/run 列表均为 0；Actions permissions 三个 GET 均 HTTP 401，因 `gh` token 无效只能记为 unknown，未读取 secrets。
- 历史关系：临时比较仓库显示本地 `099439c460584a9fda3bb7b216639181786b3fa9` 与远端 tip 无共同祖先，提交计数 `15/3`，文件树 `83/46`，差异方向 `A=0/D=37/M=16/R=0`；不可 fast-forward、不可直接合并或覆盖 `main`。
- 边界与下一步：未添加 remote、未 push、未创建分支/PR/tag/release、未触发 CI、未修改 workflow/prototype/package/lockfile；Owner 需先决定 canonical repo、历史策略、remote/fetch、分支/PR、push 和真实 Actions run 授权。详见[G1.2b-0 审计证据](./evidence/G1/2026-08-26-g1-2b-0-remote-target-audit/README.md)。

### 2026-08-26｜G1.2b-1 本地历史保全与发布前敏感审计演练完成，G1.2b 待 Owner Gate

- 当前状态不变：G1 为“执行中（G1.1 已完成，G1.2a 已完成，G1.2b 待 Owner Gate）”；本批只在 `/private/tmp` 完成临时双 parent 历史保全演练和双方完整可达 Git 历史审计，不等于远端 CI、push、PR 或 G1.2b 通过；G1 Exit 未通过，G2-A0/G2-A1/P2–P8 不打开。
- 演练结果：候选 `134b7396758792806e0e9016c924c632cd4eedea` 的 parent 1 为远端 `366ad7f287a00f795c742d7f2df10a531fa42e7c`、parent 2 为本地 `82e1090feeefb6717f8e972a0e889312f2167910`，tree `c6e69cbeb2709e3c2a7d595ba8ce5595efa99d2f` 与本地一致；两侧均为祖先，候选相对远端差异 `A=38/M=16/D=0/R=0`。该 SHA 仅为临时 rehearsal，不得未来复用。
- 完整历史审计：local `16 commits/147 blobs`、remote `3 commits/51 blobs`，双方路径风险均为 `0`；双方各有 1 条经人工分类的 `runtime_field_reference`，未发现真实 secret value；最大 blob `1,275,681 bytes`，20 MiB 超阈值均为 `0`。候选 workflow 静态存在且保持 PR 触发器与 `contents: read`。
- 边界与下一步：临时 clone/object store 已清理；未添加 remote、未 push、未创建分支/PR/tag/release、未触发 CI、未修改 workflow/prototype/package/lockfile、未连接 Preview/Supabase/生产。Owner 仍需确认 canonical repo、无共同祖先的历史策略、integration 分支/PR 与真实 Actions run 授权。详见[G1.2b-1 演练证据](./evidence/G1/2026-08-26-g1-2b-1-local-integration-rehearsal/README.md)。

### 2026-08-26｜G1.3-0 本地环境隔离、忽略规则与回退预检完成，G1.3 实施未开始

- 当前状态不变：G1 为“执行中（G1.1 已完成，G1.2a 已完成，G1.2b 待 Owner Gate）”；G1.3-0 仅完成本地 preflight，不等于 Preview 部署、在线回退演练或 G1.3/G1 Exit 通过；G2-A0/G2-A1/P2–P8 不打开。
- 忽略规则：`.env`、`prototype/.env.local`、`.vercel/**`、`.supabase/**`、node_modules、`.next`、pnpm store、tsbuildinfo 均按 `.gitignore` 忽略，`prototype/.env.example` 可跟踪；Git 当前 tree/index 无非 example 环境、部署状态、密钥证书、dump、附件或临时风险路径。ignored 运行态只统计类别/数量，未读取内容。
- Local/Preview/Staging/Production：Local 使用当前 `main`/Node `v22.12.0`/Corepack `0.34.6`/pnpm `10.33.3`、合成数据、无外部连接；Preview 未建立，Staging 未打开，Production 在本地工作范围关闭。外部资源不能由本地未绑定事实推断不存在，未有认证实时证据的状态记为 unknown。
- 版本取回：current `bfef296...` 与 workflow ref `b0681d...` 均可 archive；两者 `prototype` tree `48dd99...`、workflow blob `7f2dbd...` 一致，docs 差异预期存在；未 checkout/reset 当前分支，临时目录已清理。回退手册和矩阵见[G1.3-0 本地环境预检](./evidence/G1/2026-08-26-g1-3-0-local-environment-preflight/README.md)。
- 边界与下一步：未创建 Preview/Staging/Production、未添加 remote、未 push、未触发 CI、未读取 env/secret、未连接 Supabase/Auth/数据库或生产。Owner 需另行批准环境资源、变量/访问/数据边界、部署入口、health 与回退命令，之后才能打开 G1.3 实施。

### 2026-08-26｜G1 Exit 本地预检完成，NO-GO，Owner 决定待明确

- 本批输入：只读核对当前 `main`/HEAD、稳定 tag、G1.1、G1.2 preflight、G1.2a、G1.2b-0、G1.2b-1、G1.3-0 evidence、G1 合同、Owner 清单与本地 workflow；不修改 prototype、workflow、配置、Git refs/tags/remote 或外部状态。
- 当前结果：G1.1 本地基线、G1.2a fresh exact bootstrap/workflow 等价验证、G1.3-0 本地隔离/版本取回预检已具备；G1.3-0 已满足 Staging/Production 隔离边界合同，实际资源与高风险能力属于后续专项；G1.2b 真实 GitHub Actions run 缺失，canonical repo/Actions permissions 为 unknown，Preview 实际部署、在线 bad ref→good ref 回退和 Owner Exit 签署缺失。
- 当前状态保持：G1 为“执行中（G1.1 已完成，G1.2a 已完成，G1.2b 待 Owner Gate）”；G1.3-0 preflight 已完成、G1.3 实施未开始；G1 Exit 未通过，G2-A0/G2-A1/P2–P8 不打开。详见[G1 Exit 本地预检](./evidence/G1/2026-08-26-g1-exit-preflight/README.md)和[G1 Owner 验收清单](./stages/G1-Owner验收清单.md)。
- 未发生外部写入：本批没有添加 remote、push、分支/PR/tag/release、真实 Actions run、Preview、Supabase/Auth、数据库或生产操作；typecheck/lint/build/E2E 未重跑，复用 G1.2a 在代码/依赖/workflow 未变化时的有效证据。

### 2026-08-26｜独立审查纠正 G1 scope 与 G1.2b 授权边界

- 纠正：独立审查发现上一条 G1 Exit 预检把 Staging/Production 实际实施混入 G1 缺口，并把 G1.2b“只读认证”与后续写入动作写成不清晰边界。本条将 G1.3-0 已满足的四环境隔离合同，与后续 G2-A1/P2/P7/Production 实施、外部 provider `unknown` 盘点分开记录。
- 当前范围：G1.3-0 已满足 Staging/Production 边界合同；实际资源、Auth/DB/RLS/PII、监控、备份/恢复属于后续专项，不是 G1 Exit 前置。外部 provider/资产认证只读盘点仍须在选择 Preview provider 前完成，但不授权创建或连接资源。
- G1.2b 授权入口：只有 Owner 明确采纳[G1 Owner 验收清单](./stages/G1-Owner验收清单.md)与[G1 Exit 本地预检](./evidence/G1/2026-08-26-g1-exit-preflight/README.md)中的精确语句后，才解除一次受限 integration/PR/Actions 流程的旧 remote/push 禁止；当前 G1.2b 仍待 Owner Gate，G1 Exit 未通过，G2-A0 不打开。

### 2026-08-26｜GOV-1 当前状态去漂移审计完成

- 事实：对非权威文档的当前时态、G1 Exit 预检标题、GOV-1 阶段索引和本台账导航做了只读审计；发现并纠正账号合同、V1 范围、设计基线、架构/A1 入口、连接骨架和 UI 映射中的陈旧当前措辞，保留带日期的历史决定与证据原意。
- 当前状态不变：GOV-1 已通过；G0/P1 已通过并冻结；G1 仍为“执行中（G1.1 已完成，G1.2a 已完成，G1.2b 待 Owner Gate）”；G1.3-0 本地预检已完成、G1.3 实施未开始；G1 Exit 未通过；G2-A0/G2-A1/P2–P8 不打开。
- 证据与边界：本批只改 Markdown、未修改 prototype、package/lockfile、workflow、配置或 Git refs/remote；详见[GOV-1 当前状态去漂移审计](./evidence/GOV-1/2026-08-26-current-state-drift-audit/README.md)。

> 以下带日期的 G1/G2-A0 条目均为历史快照；当前状态以本台账顶部状态表和下方 2026-08-28 当前状态纠正为准。当前 G2-A1 资源事实与窄范围 Gate 见[G2-A1 资源预检证据](./evidence/G2-A1/2026-08-28-entry-preparation/README.md)。

### 2026-08-26｜G2-A0 Entry preflight 安全补充完成（历史快照；政策落账前），G2-A0 仍未开始

- 当前状态不变：G1 Exit 仍为 NO-GO；G2-A0 仍为“未开始”，G2-A1、P2–P8 不打开。本批只准备 A0 安全合同、Owner 清单和 Entry preflight 证据，不授权正式 G2-A0 审查或任何 Auth/数据库/Storage 实现。
- 预检补充：按独立只读安全审查发现，补齐删除/暂停后的 session/token 边界（A1 观察 session/token 时间窗口；A3/A4 验证业务拒绝与跨租户负向）、Data API grants 与 RLS 两层、exposed allowlist、UPDATE 的 SELECT/USING/WITH CHECK、view `security_invoker`、`SECURITY DEFINER` 约束、Storage upsert 权限、managed schema 限制、SSR client/Proxy/cookie refresh 并发边界和 provider/plan/region/session 矩阵；Entry preflight 提出候选产品边界：建议 V1 暂不采用 phone/SMS MFA 与静态恢复码，均待 G2-A0 Owner 确认，供应商能力仍待 A1 验证；A1/A3/A4 实测仍未开始。
- 证据与边界：官方页面只读复核，未连接或创建 provider/project；详见[G2-A0 Owner 验收清单](./stages/G2-A0-Owner验收清单.md)与[G2-A0 Entry preflight](./evidence/G2-A0/2026-08-26-entry-preflight/README.md)。只有 G1 Exit 明确通过并经 Owner 签署，才可进入正式 G2-A0 安全审查。

### 2026-08-27｜G1.2b 真实 PR/CI 完成，G1 Exit 仍 NO-GO

- Owner 授权原话：`我确认允许将当前 Rebuy 本地仓库的源码、项目文档、Git 历史和图片公开上传到公开仓库 kyox215/REBUY_SHARE；允许推送 integration/g1-2b、创建 PR、运行 GitHub Actions，验证通过后以非强制方式合并到远端 main；禁止 force-push、删除远端历史、部署 Preview、连接 Supabase/Auth/DB 或生产。`；另有本次 Workflow scope 限定，均只用于本仓库本次 G1.2b。实际本批未合并 `main`，等待独立复审。
- 远端事实：`kyox215/REBUY_SHARE` 为 public canonical repo；远端 `main=366ad7f287a00f795c742d7f2df10a531fa42e7c` 未漂移；双 parent integration 初始 head `f746dcacc0afc2d45b847346f20078a159c2e032` 保留远端与本地历史；PR #1 base 为 `main`，head 为 `integration/g1-2b`，当前 OPEN/CLEAN。
- 真实 CI：run [33027593355](https://github.com/kyox215/REBUY_SHARE/actions/runs/33027593355)，job [98372467897](https://github.com/kyox215/REBUY_SHARE/actions/runs/33027593355/job/98372467897)，由 `pull_request` 触发并验证 head `f746dca...`；install、typecheck、lint、build 和收尾步骤全部 success。
- Actions 只读设置：enabled `true`、default workflow permissions `read`、allowed actions `all`、SHA enforcement `false`、PR approval `false`；`selected-actions` 在 all 策略下返回 409，按 `all` 记录。仓库级允许范围过宽是后续治理风险，workflow 自身仍使用完整 action SHA 和 `contents: read`。
- 审计与质量：本地 `main` 可达历史为 26 commits/206 blobs，远端 `main` 为 3/51，integration 候选为 30/227；禁止路径风险（排除允许的 `prototype/.env.example`）均为 0，字面 secret pattern 均为 0，最大 blob 为 1,275,681 bytes，20 MiB 超阈值为 0；既有 Node `22.12.0`、npm `10.9.0`、Corepack `0.34.6`、pnpm `10.33.3` 本地 install/typecheck/lint/build 均通过。
- 当前影响：G1.2b 真实 PR/CI 已完成，但 PR 尚未合并；G1.3-0 仍仅为本地预检，G1.3 实施未开始；Preview、在线 bad ref→good ref 回退、Owner G1 Exit 签署仍缺失，故 G1 Exit 继续 `NO-GO`，G2-A0/G2-A1/P2–P8 不打开。完整脱敏证据见[G1.2b 远端 PR/CI](./evidence/G1/2026-08-27-g1-2b-remote-ci/README.md)。
- 边界：本批只公开推送 integration 分支、创建 PR 并运行一次初始真实 Actions；未 force-push、未删除远端历史、未合并 `main`、未读取 secrets、未部署 Preview、未连接 Supabase/Auth/DB、Staging 或 Production。文档提交后的新 head 只需以 PR 当前 check 独立复核，不为回写新 run ID 递归修改本记录。

### 2026-08-27｜G1.2b PR #1 main merge closeout，G1 Exit 仍 NO-GO

- Owner 已授权在独立复审明确 GO 后，以非强制 PR merge 方式写入远端 `main`；独立复审已对 PR #1 head `0bb5fb527d51a304b95b05794345bd23128e1534` 正式给出 GO。未使用 direct push main、squash、rebase、force-push，未删除 `integration/g1-2b`。
- 合并前只读确认远端 `main=366ad7f287a00f795c742d7f2df10a531fa42e7c`、PR #1 `OPEN/CLEAN`、base=`main`、head=`integration/g1-2b`；通过 GitHub PR merge commit 合并后，远端 `main=cba97eb4e7c93e5c42ba496e4d2ddeac5b476c7c`，`integration/g1-2b` 仍保留。
- main push 触发 GitHub Actions run [33031297793](https://github.com/kyox215/REBUY_SHARE/actions/runs/33031297793)，job [98384190584](https://github.com/kyox215/REBUY_SHARE/actions/runs/33031297793/job/98384190584)；install、typecheck、lint、build 和收尾步骤全部 SUCCESS。main merge 与 workflow 结果的脱敏 closeout 见[G1.2b main merge evidence](./evidence/G1/2026-08-27-g1-2b-main-merge/README.md)。
- 当前状态：G1.2b 真实 PR/CI 已完成并合并 main；G1.3-0 仍仅为本地预检，G1.3 实施仍待 Owner Gate；Preview、在线 bad ref→good ref 回退和 Owner G1 Exit 签署仍缺失，故 G1 Exit 保持 `NO-GO`，G2-A0/G2-A1/P2–P8 不打开。
- 边界：本次只完成已授权 PR merge 与 main push CI 验证；未部署 Preview、未连接 Supabase/Auth/DB、Staging 或 Production，未读取 secrets/PII。Actions `allowed_actions=all`、SHA enforcement=`false` 的治理债务仍保留，后续由 Owner/治理专项决定是否收紧。

### 2026-08-27｜G1.3-1 provider/CLI/部署前隔离预检完成，G1.3 待 Owner Gate

- 本批只读核验现有 Vercel 归属：team [authorized Vercel team redacted] / `team_AOJDnrjov0QDLqpvMyhwA1yc` / Pro；project `rebuy-share` / `prj_g1W3AWm3hkbZib9zDgm6YQfGEyHL`；仅有旧 `READY`、`target=production` deployment `dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH`；项目 Node `24.x`、无 Git link。本批未修改项目设置、部署、Production alias 或费用配置。
- Supabase 只读 inventory 没有发现已获批准、可用于 Rebuy 的独立 non-production project；任何无关项目禁止复用。本批未创建、连接或触碰任何 Supabase/Auth/DB/Storage 资源，未读取或记录组织/项目名称、ID、数量、套餐、host、region、key、token、环境值或 PII。
- 当前 `main@af6d7419956ce6640c0b4af5df4db0369e793f77` 的归档副本在 Node `22.12.0`、Corepack `0.34.6`、pnpm `10.33.3` 下完成 frozen install、typecheck、lint、build，均退出 0；临时目录已清理，仅保留 `unrs-resolver` ignored build-script warning。main Actions run [33034314565](https://github.com/kyox215/REBUY_SHARE/actions/runs/33034314565) / job [98393579170](https://github.com/kyox215/REBUY_SHARE/actions/runs/33034314565/job/98393579170) 已 SUCCESS。
- Vercel CLI `53.1.1` 已只读核对 `deploy/link/inspect --help` 的 `cwd/scope/target/project/team/yes/wait/timeout/format` 语法；`whoami` 无可确认身份输出；未 login、link、deploy、inspect、promote、rollback，也未写 `.vercel`。
- 当前 Gate（G1.3-2 审查前快照）：G1.3-1 provider/CLI/本地隔离预检完成；G1.3 实施待 Owner Gate，G1 Exit 仍 `NO-GO`，G2-A0/G2-A1/P2–P8 不打开。该快照原拟要求先明确 Node `24.x → 22.x` 候选变更；随后 G1.3-2 已纠正为 project Node24 默认不 PATCH，由 `engines.node=22.x` 请求 Node22 并以 build log 实证；其余仍仅以已通过 CI 的 `main@af6d741`、精确 archive 的 `prototype/` cwd、Target Preview 执行，不改变 Production alias、不注入任何 Supabase/Auth/DB/PII 或环境值，并另行受控演练 bad503 → good200。
- 完整脱敏记录见[G1.3-1 provider/Preview 部署前预检](./evidence/G1/2026-08-27-g1-3-1-provider-preview-preflight/README.md)。

### 2026-08-27｜G1.3-2 Preview 执行高风险独立审查完成，原方案 NO-GO、修正后条件 GO

- 独立 reviewer 针对 Preview 部署、Node、project `rootDirectory`、变量存在性、Deployment Protection、Production aliases 和在线回退完成高风险审查：原执行草案为 `NO-GO`；修正后仅为条件 `GO` 候选，不能写成 Preview 或 G1.3 已通过。
- 历史审查快照（已废止）：当时修正合同草案曾写作仅从精确 `main@af6d741` archive 的 `prototype/` cwd 执行、project `rootDirectory` 保持未设置；当前执行合同为 provider `rootDirectory=prototype` 保持不变，从精确 archive 仓库根执行 CLI，禁止从 archive/prototype；Preview 前只列变量名/target，任一应用变量存在即停止；不关闭 Deployment Protection，使用 `vercel curl`；旧 production deployment 与 aliases fingerprint 前后不变。
- 回退合同：`bad503` 使用独立 `DO NOT MERGE` Draft PR，永不 Ready/merge，关闭 PR 但保留分支；`good200` 及后续永久 good route 另从干净 main/最终集成分支进入独立普通 PR。该审查快照当时仍无外部执行授权、未登录、未 push、未创建 PR、未 deploy；G1 Exit 继续 `NO-GO`。
- 完整记录见[G1.3-2 Preview execution review](./evidence/G1/2026-08-27-g1-3-2-preview-execution-review/README.md)。

### 2026-08-27 10:33:59 CEST｜G1.3 修正版 Owner Gate 已批准，implementation 执行中

- Owner 原话：`我批准 ，下次无需我批准`。该回复语义明确承接主线程上一条完整 G1.3 修正版授权语句，批准受限 G1.3 implementation 范围，不是 G1 Exit 签署，也不把条件 GO 候选写成已完成。
- 授权范围：允许 Vercel OAuth 临时认证；仅限上一条完整授权语句指定的两个 `codex/*` 分支/PR/Actions 入口（本记录不臆补名称或编号）；最多三个 `Target=Preview`，角色仅为 `bad503`、`good200`、`final`；允许 Vercel Pro 正常 build/compute 用量；在该范围内风险触发时可受限取消构建中的 Preview。OAuth/login 是否发生、分支/PR/Actions 是否创建及 Preview 是否部署，均须由后续证据确认。
- 原修正合同保持不变：`engines.node=22.x` 覆盖 project Node24，Node 默认不 PATCH，需以 deployment provider-resolved config `builds[0].config.nodeVersion=22.x` 为主证据，build log 仅作补强；resolved 缺失或非 22 立即 STOP；provider `rootDirectory=prototype` 保持不变，从精确 archive 的仓库根执行 CLI，禁止从 archive/prototype 执行；Preview 前只列变量名/target，任一应用变量存在即停止；不关闭 Deployment Protection，使用 `vercel curl`；旧 production deployment `dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH` 与 aliases fingerprint 前后必须不变；bad503 使用永不 Ready/merge 的 `DO NOT MERGE` Draft PR，关闭 PR 但保留分支，good200 与后续永久 good route 另用独立干净普通 PR。
- `下次无需我批准` 仅表示上述已批准范围内的常规可回退步骤和必要重试不重复询问；不得扩展到未来新付费资源、Supabase project/cost、任何 secret/env 值、Auth/DB/PII、支付、Staging/Production、真实业务数据、Production alias/promote/rollback、项目删除或其他 team/project 设置。
- 当前状态：G1.3 implementation 从“待 Owner Gate”更新为“已授权/执行中”；bad503 Preview 已按独立门禁完成并保留 evidence；历史代码候选快照 ref=`5ce3723b73edcd7284f88b26d6faa0e31ed01b40` 曾为 PR#5 head，首轮 Actions run=`33074662873`/job=`98525606734` 的 install/typecheck/lint/build 已成功；good Preview source ref=`f4225397dc6c6b99e315d5ca4a7ecbc8695fb529`（当时 PR#5 current head）的 exact-head Actions run=`33078824609`/job=`98540116896` 的 install/typecheck/lint/build 已成功，随后部署 good deployment `dpl_D2oNMJhvQsvbbyszgApm24aGLYnZ` 并完成 bad→good 在线恢复技术 GO；本次 docs-only closeout 会生成后继 head，其 SHA/run 不预写、不递归回写；当前仅待 docs-only closeout 后新 current-head Actions 的 install/typecheck/lint/build 全部成功、关闭 bad 独立 Draft PR 并保留分支、merge 授权/复审及 Owner G1-19；不再等待 good Preview deploy 门禁；既定门禁外不创建额外 Preview；不得将 bad503/good200 结果外推为 G1 Exit 通过，G1 Exit 保持 `NO-GO`，G2-A0 不打开。

### 2026-08-27｜G1.3 外部 provider/Preview preflight 收口（GO）

- 本批只读确认 GitHub `origin/main=af6d7419956ce6640c0b4af5df4db0369e793f77` 无漂移；latest main Actions 的 `head_sha` 与该 SHA 相符、状态 `success`，对应 run `33034314565` / job `98393579170`。
- Vercel team `team_AOJDnrjov0QDLqpvMyhwA1yc`、project `prj_g1W3AWm3hkbZib9zDgm6YQfGEyHL` 已按最小字段只读确认；default CLI existing auth 用户为 `kyox120-9295`。未创建新 OAuth token；device UI 未获授权且未记录凭据。project Node `24.x` 与 provider `rootDirectory=prototype` 均保留不变，Git link absent。
- `package.json` `engines.node=22.x` 覆盖 project Node24，正式策略为默认不 PATCH；未来 Preview 必须从 exact git archive 的仓库根执行 CLI，禁止从 archive/prototype 执行以免 `prototype/prototype`，并以每次 deployment provider-resolved config `builds[0].config.nodeVersion=22.x` 为主证据、build log 仅作补强；resolved 缺失或非 22 立即 STOP。
- Preview env 只读脱敏结果为 `0`；未读取或记录任何 value。Deployment Protection 为 `Standard` + `Require Log In`，未关闭或改弱。
- 旧 production deployment `dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH` 仍为 `target=production`、`READY`，latest production ID 相同；production aliases 数量 `2`，排序后 SHA-256=`c06eeb6c408c562d7d6906cf1ccd71776beea381afae47e94547c696133f79aa`。不记录 alias 字符串或 URL。
- G1.3 external preflight 与 rehearsal Entry 门为 `GO`，仅表示 provider/隔离/执行入口可按合同继续；bad503 Preview 已另行按独立门禁完成；不表示 good200 Preview 已部署、在线 bad→good 已完成或 G1 Exit 通过。G1 Exit 继续 `NO-GO`，G2-A0 不打开。
- preflight 阶段未执行或伪造 good200 的 push、PR、Actions、Preview、deploy 结果；bad503 Preview 后续已按独立 evidence 完成；未做 link、项目设置、env、Protection、alias、promote、rollback 或其他外部写入。临时 env 读取目录 `/private/tmp/rebuy-g13-preview-env-read-20260827-1050` 保留且不含敏感值记录。

### 2026-08-27｜G1.3-3 good Preview 验收与 bad→good 技术恢复完成，G1 Exit 仍 NO-GO

- source ref：`f4225397dc6c6b99e315d5ca4a7ecbc8695fb529`；该部署时 PR#5 current head 为同一 SHA；该 exact head 的 Actions run=`33078824609`/job=`98540116896` 的 install/typecheck/lint/build 已成功，随后部署 good deployment `dpl_D2oNMJhvQsvbbyszgApm24aGLYnZ` 为 project `rebuy-share` 的唯一 Target=Preview/READY；resolved Node22、root/health HTTP 200、body 仅 `{ "status": "healthy" }`、Cache-Control 含 `no-store`；Protection `all_except_custom_domains`、Preview env=0；bad→good 在线恢复技术 GO；PR#4/PR#5/main/Production 不变量保持；good Preview 已完成。
- 固定 Node `22.12.0`、Corepack `0.34.6`、pnpm `10.33.3` 下，`pnpm install --frozen-lockfile`、`pnpm typecheck`、`pnpm lint`、`pnpm build` 均退出 `0`；仅保留 `unrs-resolver@1.12.2` ignored build-script warning，未执行 approve-builds/allowlist。
- 本地 production server 使用 `next start -p 3104` 后已安全停止：`/` HTTP `200`，`/api/health/app` HTTP `200`，body 仅 `{"status":"healthy"}`，`Cache-Control: no-store`；route scope 敏感扫描为 `0`。docs 中历史 bad503 的 `503/unhealthy` 仅作外部 evidence，不纳入 route scope 结论。
- bad503 外部结果复用已归档的 deployment `dpl_J9E3WThCmtqxAndfjQDKAW1G49EU`：Preview `READY`、resolved config 为 `builds[0].config.nodeVersion=22.x`、根页 `200`、health `503/unhealthy/no-store`、Protection 与旧 Production alias count/fingerprint 不变量均已通过；不保存 URL、alias、token、cookie 或环境值。
- 当前已完成 good200 本地候选、首轮 PR#5 Actions、good Preview READY 与 bad→good 在线恢复技术 GO；final Preview 默认不重复（docs-only 且 code/config/lock 未变，上限非强制）；本次 closeout 后继 SHA/run 不预写、不递归回写；任何 merge 前必须实时确认 PR 当前 head 及其新 Actions 的 install/typecheck/lint/build 全部成功，否则 STOP；G1 Exit 继续 `NO-GO`，G2-A0/G2-A1/P2–P8 不打开。

#### good Preview closeout 与部署不变量

- deployment inventory（只读）仅包含 good Preview `dpl_D2oNMJhvQsvbbyszgApm24aGLYnZ`、bad Preview `dpl_J9E3WThCmtqxAndfjQDKAW1G49EU` 与旧 Production `dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH`；本批未发现其他 deployment。
- good Preview 的 provider-resolved config 为 Node `22.x`；根页 HTTP `200`；`/api/health/app` HTTP `200`；body 仅 `{ "status": "healthy" }`；Cache-Control 含 `no-store`。
- Preview Protection 为 `all_except_custom_domains`；Preview env 数量=`0`；未读取、记录或注入任何环境值。
- Production 不变量保持：旧 Production target/READY 未变，alias count=`2`，normalized mapping SHA-256=`c06eeb6c408c562d7d6906cf1ccd71776beea381afae47e94547c696133f79aa`；不记录 alias 字符串、URL、token 或 cookie。
- PR/main 不变量保持：bad PR#4 仍为 Draft、永不 Ready/merge；PR#5 当时 current head 为 `f4225397dc6c6b99e315d5ca4a7ecbc8695fb529`；`main` 未改变；未 promote、切换 alias、rollback 或写入 Production。
- bad→good 在线恢复技术 GO 仅表示本次受控技术恢复的 root/health 200、healthy/no-store 与 READY 证据成立，不代表 G1 Exit 或 Production 验收。
- final Preview 默认不重复：本次为 docs-only closeout，code/config/lock 未变化；Preview 上限为非强制上限，不新增 final deployment。
- closeout lineage：本次 closeout 后继 SHA/run 不预写、不递归回写；任何 merge 前必须实时确认 PR 当前 head 及其新 Actions 的 install/typecheck/lint/build 全部成功，否则 STOP。

如发现状态或历史事实错误，新增“日期｜纠正”条目并链接到阶段记录；不得删除或静默改写既有决定。

### 2026-08-27｜G1.3 bad503 Draft PR closeout 完成，G1 Exit 仍 NO-GO

- 本条为本台账最新的 G1.3 closeout 状态；早期带日期的审查/预检快照保留其当时事实，不覆盖本条当前结论。
- close-bad 门已完成：PR#4 状态为 `CLOSED`，仍为 `Draft`；head=`059c936c5ecdf4152141ed685fa64151b22e3326`，merge commit=`null`，comments=`0`；远端 bad 分支保留该 head；不 Ready、不 merge、不删除分支。
- provider/Production 不变量保持：deployment inventory（只读）仅包含 bad Preview `dpl_J9E3WThCmtqxAndfjQDKAW1G49EU`、good Preview `dpl_D2oNMJhvQsvbbyszgApm24aGLYnZ` 与旧 Production `dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH`，共 `3` 项且各自 `READY`；good Preview Protection=`all_except_custom_domains`、Preview env=`0`；Production alias count=`2`，normalized mapping SHA-256=`c06eeb6c408c562d7d6906cf1ccd71776beea381afae47e94547c696133f79aa`；`main=af6d7419956ce6640c0b4af5df4db0369e793f77` 未改变；不记录 alias 字符串、URL、token、cookie 或环境值。
- PR#5 当时以 head=`b02715be` 保持 `OPEN`、非 Draft、`mergeable`；该 exact-head Actions run=`33084137265` / job=`98559030766` 的 install/typecheck/lint/build 已成功。该事实为代码候选快照，不预写 docs-only closeout 后继 head/run。
- 当前仅待 docs-only closeout 后新 current-head Actions、independent merge review、Owner G1-19 与明确的 main merge 授权；不得预写 merge，G1 Exit 继续 `NO-GO`，G2-A0 不打开。

### 2026-08-27｜G1.3 technical closeout 通过，G1 Exit GO，G2-A0 已授权打开

- Owner 原话：`确认 G1.3 technical closeout 通过；验收 ref=d51f1c7cb47e2fe2932b29bd39420f5d092a8160；签署 G1-19，G1 Exit GO，日期 2026-08-27，并授权打开 G2-A0。授权从该 main 新建 codex/g1-final-closeout docs-only 分支，非强制 push、创建 PR 和运行 Actions；若差异仅为批准的文档、exact-head Actions 与独立复审通过，允许以 merge commit 合并 main。禁止 squash、rebase、force/direct push、删除分支或 deployment，以及 Production/promote/deploy/Supabase/Auth/DB 操作。`
- G1-19 已满足，G1 Exit 当前为 `GO`，验收 ref=`d51f1c7cb47e2fe2932b29bd39420f5d092a8160`；本条覆盖前文 closeout 前的 `NO-GO/待签署`快照，不删除或静默改写历史。
- 已确认 main 谱系：PR #5 merge=`d51f1c7cb47e2fe2932b29bd39420f5d092a8160`，parents=`af6d7419956ce6640c0b4af5df4db0369e793f77` + `824dd27f37792b3f487ec7a9ab21270b4b97fb84`；main Actions run=`33089108238` / job=`98576781415`，install、typecheck、lint、build 四步均 success。
- `codex/g1-final-closeout` 已按 Owner 指定为 docs-only 关闭批次入口；该分支后继 SHA、PR、exact-head Actions、独立复审和 merge commit 在实际事件发生前均不预写，必须在实时门通过后记录。
- G1.3 technical GO 的既有不变量保持：bad PR #4 及 bad 分支/Preview evidence 保留；good/bad Preview、Production asset/aliases fingerprint、Deployment Protection、provider `rootDirectory=prototype` 和 Preview env 等均未因本批文档变更而改变。未新增 Preview、未 promote、未 deploy。
- 当时记录为“准备中”：已授权并打开准备入口，但尚未实施 A0 安全审查、Auth、DB、Storage、RLS 或任何外部资源写入；随后在本台账 2026-08-27 G2-A0 Entry 条目中更新为“执行中”。G2-A1 仍为“未开始/未打开”。Production、Supabase、Auth、DB、secret/env 值、PII 和真实数据继续禁止。
- 关联记录：[G1 final closeout evidence](./evidence/G1/2026-08-27-g1-final-closeout/README.md)、[G1 Owner 验收清单](./stages/G1-Owner验收清单.md)、[G1 工程合同](./stages/G1-工程底座与环境隔离.md)。

### 2026-08-27｜G2-A0 账号安全合同与威胁模型 docs-only 执行启动

- Owner 已确认 G1.3 technical closeout 通过，签署 G1-19、G1 Exit=GO（日期 `2026-08-27`，验收 ref=`d51f1c7cb47e2fe2932b29bd39420f5d092a8160`），并授权打开 G2-A0。
- 本批从已确认 `main@7ea1e45ad22ab29105910665baf4bbd7212241c5` 建立本地 `codex/g2-a0-security-contract` docs-only worktree；仅允许更新账号安全合同、威胁模型一致性、Owner Gate、阶段台账和导航。
- （历史，政策落账前）本批仅同步 07/08 状态头，不修改其中 phone/SMS MFA、静态恢复码、AAL2、双人复核等当时 Owner 未决结论；不重复新建 ADR、PRD、完整权限矩阵或威胁模型。
- （历史，政策落账前）G2-A0 当前状态为“执行中”，但 Exit 尚未通过；独立安全审查和 Owner 决策矩阵仍待完成。G2-A1 保持“未开始”，不得因本批文档执行自动打开。
- 本批不修改代码、依赖、lockfile、workflow、环境配置或生成物；不连接/写入 Supabase/Auth/DB/Storage/Realtime，不读取 secret/env/PII，不 push、不开 PR、不运行远端 Actions、不部署或修改 Production。
- 详细事实、验证、风险与回退见 [G2-A0 阶段记录](./stages/G2-A0-账号安全合同与威胁模型验收.md) 和 [G2-A0 Owner 清单](./stages/G2-A0-Owner验收清单.md)。

### 2026-08-27｜G2-A0 Owner 七项政策已采纳；Exit 待新 exact-head 签署（历史快照）

- Owner 原话：`G2-A0 七项政策全部采用推荐方案：排除 phone/SMS MFA；排除静态恢复码并采用异地备用 TOTP＋受审计人工恢复；高权限角色按推荐时点强制 AAL2；六类高风险操作实行双人复核；A1 优先验证 Supabase、Clerk/Auth0 仅比较；plan/region/session 留待 A1 按 EU 非生产环境实测决定；GDPR及税务法律问题转交 A5 和专业顾问。授权写入最终文档；G2-A1 的资源、费用、secret、Auth、DB、Storage、OAuth、SMTP、部署和 Production Gate 继续关闭。`
- 七项当前决定：V1 排除 phone/SMS MFA；排除静态恢复码并使用不同设备/安全位置备用 TOTP＋受审计人工恢复；平台 owner/admin/reviewer 上线前、商家 owner/admin 最迟 Beta 前强制 AAL2，普通买家按风险提升，高风险动作重新认证；MFA 人工恢复、owner 转移、角色/权限策略变更、敏感导出、隐私删除/导出、证明文件高风险访问六类动作双人复核且禁止自审；A1 优先验证 Supabase、Clerk/Auth0 仅比较；plan/region/session 由 EU non-production A1 实测决定；GDPR/税务留存、删除、legal hold、跨境和处理者合同转 A5 与专业顾问。
- decision-ready baseline `9b11f375080db68353dd6952774bcd5e75c4153c` 的独立审查 findings=`none/GO`，范围是文档治理一致性，不是 Auth、MFA、DB、RLS、Storage 或其他运行时测试；新 exact-head 仍需主代理安排复审。
- 当前状态保持：G2-A0 `执行中`；七项政策已采纳，G2-A0 Exit 待新的 exact-head 复审与 Owner 签署。G2-A1、P2–P8 未打开；non-production project/resource、费用、secret、OAuth、SMTP、Auth、DB、Storage、部署和 Production 连接仍需独立授权，本次不授权。
- A/P 映射债务仍存在：必须在打开 P2 前由 Owner/主计划明确 A0–A6 与 P0–P8 映射；不得从 A1 自动打开 P2/P3+。

### 2026-08-27｜G2-A0 Exit GO；远端 reconciliation 历史快照

- Owner 最新原话（逐字记录）：`不在需要我批准  你自己决定 我都同意`。该原话承接紧前完整 G2-A0 授权文本；仅表示在已批准、可回退的 docs-only reconciliation 范围内不再重复请求批准，不扩大为未来付费资源、真实 PII、Auth/DB 或 Production 的永久豁免。
- Owner 已签署 G2-A0 Exit GO，日期 `2026-08-27`，验收 ref=`140ea15d9c3f178a326709d35ad1750a156df0d1`。批准范围包括将本阶段 12 个 Markdown、相关 Git 历史、Owner 姓名、账号安全架构、威胁模型、角色权限和阶段治理信息公开到 `kyox215/REBUY_SHARE`；允许非强制 branch push、PR、`prototype-quality` Actions；仅在批准 docs-only 差异、exact-head Actions 成功和独立复审通过后，以 merge commit 合并 `main`。
- 远端写入前 preflight（140ea 历史）：远端 `main=7ea1e45ad22ab29105910665baf4bbd7212241c5`，目标 branch/PR 无；公开外发审计为 12 个 Markdown、`351437` bytes，无 binary/image/secret/phone/address/customer PII，且新增内容审计未发现新增邮箱但漏计继承内容。base/public main 及既有 Git author metadata 已含同一 Owner 邮箱，G2-A0 未引入不同邮箱；当前 G2-A0 12 路径候选中 docs/15 的 Owner 邮箱正文已脱敏；未改动的既有历史/evidence 文档及 Git author 历史可能仍含同一邮箱；不可声称 Git 历史无 email。Owner 此前明确授权公开仓库 Git 历史且最新消息同意当前闭环；此前 push 审批被拒，远端写入为零。
- `61746d140742564f043121185f1b56bbdca3fb5a` 的 pre-review audit 为 12 个 Markdown、`366645` bytes；该数值只绑定该 617 pre-review audit，不代表本次修复后的新提交。提交后由外部复审重新计算最终字节数，本提交不自引用最终字节数。
- 本地 closeout 仅在 `codex/g2-a0-security-contract` 追加 docs-only 提交。前一 exact-head `140ea15d9c3f178a326709d35ad1750a156df0d1` 独立文档治理审查 findings=`none/GO`，不等于运行时测试；本次新 closeout head 尚待独立复审，未预写 PR、Actions run/job 或 merge 结果。
- G2-A1、P2–P8 仍为“未开始”；resource/cost/secret、Supabase/Auth/DB/Storage/OAuth/SMTP、真实账号/真实 PII、Preview/Production deploy、promote、alias、rollback 和生产写入 Gate 继续关闭。A0 Exit 仅打开 A1 准备门，资源授权另行处理。

### 2026-08-28｜G2-A0 公开外发证据纠正与 closeout 修订

- 纠正事实：`140ea15d9c3f178a326709d35ad1750a156df0d1` 的新增内容审计未发现新增邮箱，但漏计继承内容；base/public main 及既有 Git author metadata 已含同一 Owner 邮箱，G2-A0 未引入不同邮箱。Owner 此前明确授权公开仓库 Git 历史且最新消息同意当前闭环。
- 隐私边界：当前 G2-A0 12 路径候选中 docs/15 的 Owner 邮箱正文已脱敏；未改动的既有历史/evidence 文档及 Git author 历史可能仍含同一邮箱；不可声称 Git 历史无 email。不得复制邮箱原值或 hash。
- 字节证据绑定：`351437` bytes 只绑定 140ea 历史 preflight；`366645` bytes 只绑定 `61746d140742564f043121185f1b56bbdca3fb5a` 的 pre-review audit。修复提交后的最终字节数须由提交后外部复审重新计算，本提交不自引用最终字节数。
- 当时状态：G2-A0 Exit GO 的 Owner 签署日期为 `2026-08-27`；在该历史快照时远端 docs-only reconciliation 已获批但尚未执行，本次纠正提交后的新 head 待独立复审。该条只保留当时事实，不代表当前状态；G2-A1/P2+、resource/cost/secret、Supabase/Auth/DB/Storage/OAuth/SMTP、真实账号/PII、部署和 Production Gate 继续关闭。

## 5. 2026-08-28｜G2-A0 远端闭环完成；G2-A1 无资源 Entry preparation（历史快照；当前资源预检见下方）

- G2-A0 远端 reconciliation 已完成：PR #7 merge=`fd9b712c7b07bf34399f9838eebb75846425c1d1`，parents=`7ea1e45ad22ab29105910665baf4bbd7212241c5` + `1433e7c7c141df0f5498fff7cd645a8d5c92340c`；main Actions run=`33122238997` / job=`98691703085` 的 install、typecheck、lint、build 全部 success；merge SHA 的 GitHub deployments=`0`；来源分支保留。完整脱敏摘要见[G2-A1 Entry preparation 证据](./evidence/G2-A1/2026-08-28-entry-preparation/README.md)。
- Vercel 仍为 `3` 个既有 deployments；Production `dpl_DZSmbtizfp3z7x2X4itwdwyLGxrH` 仍 `READY`/`PROMOTED`，alias 未变，Git link 为空，无新 deployment。项目设置 Node `24.x` 与既有 Production 构建元数据 Node `22.x` 的差异仍是遗留风险，本批未改变。
- （历史快照）G2-A1 当时正式状态：**技术阶段未开始；无资源 Entry preparation 已完成/待审；resource/cost/secret Gate 关闭**。本批仅完成文档、模板、官方来源刷新和 `.invalid` 合成字段定义；不创建/连接 Supabase/Auth/DB/Storage/OAuth/SMTP，不读取 secret/env/PII，不创建真实 fixture，不部署、不修改 Production。
- 允许公开的 Supabase inventory 摘要仅为：只读 inventory 没有发现已获批准、可用于 Rebuy 的独立 non-production project；任何无关项目禁止复用。本树不公开 Supabase inventory 的组织/项目名称、ID、数量、套餐、host 或现有 region。候选仅为 proposal：在 Owner 明确选择的 organization 内新建独立 Rebuy non-production project；计划须支持 A1 必测能力（session lifetime/single-session 因官方限制需 Pro 或更高）；候选 `eu-central-1`、无 add-on、synthetic-only，organization-level Spend Cap 状态另行只读核验。用户先选 organization + resource scope；provider `get_cost` 只返回并确认其实际提供的 amount/recurrence，tax/VAT/billing-address effect 另由 billing page/专业顾问核对，Spend Cap 状态/覆盖范围另行核验；三项完成并由 Owner 确认后才可创建。
- [G2-A1 准备与资源门禁](./stages/G2-A1-Auth-Spike准备与资源门禁.md)列出四批执行合同、责任字段、停止/回退和 2026-08-28 官方来源刷新；[资源成本与密钥 Gate 模板](./templates/G2-A1-资源成本与密钥Gate模板.md)和[Auth 实测矩阵模板](./templates/G2-A1-Auth实测矩阵模板.md)是后续专用模板。A1 Gate 通过前不得进行 Auth/DB/Storage/secret/费用或部署操作；A1 通过也不自动打开 P2/P3+。

### 2026-08-28｜G2-A1 Free Supabase 资源存在性/基础预检完成（历史资源快照；当前 B1 状态见下方）

本条是本节无资源 Entry preparation 之后的当前纠正，取代其中“尚未创建独立资源”的当前性表述；不删除历史准备事实。

- 独立 Supabase 组织 `Rebuy Lab` 已创建，connector 核验 `plan=Free`；独立项目 `rebuy-auth-spike` 已在 `eu-central-1`（Frankfurt）创建，限定用于 EU non-production Auth spike，数据模式为 synthetic-only。
- provider project quote 实际返回 `amount=0`、`recurrence=monthly`；API 未返回 currency。Owner 已确认该实际 quote，随后已完成 `confirm_cost`。这不等于税费、Spend Cap 或完整成本责任确认。
- （历史运行窗口）项目管理面状态为 `ACTIVE_HEALTHY`；仅证明当时资源存在且管理面健康，不证明 Auth、MFA、session、DB、RLS、Storage、OAuth、SMTP 或 SSR 已验证。独立运行时复审期间 connector 无法再次列出精确目标，当前管理面状态无法复验。
- （资源预检历史时点）当时没有读取、记录或传播 secrets、keys、passwords、环境变量值、host、URL、project ref、组织/项目 ID 或其他账号资源标识；后续 B1 仅按授权将原值存入 gitignored local env，执行结果见下方当前条目。没有配置 Auth/DB/Storage/OAuth/SMTP，没有建表、写数据、创建真实账号、部署、promote、alias 或 Production 操作。
- Gate 已在窄范围完成资源存在性/基础预检与 B1 最小连接验证；完整 resource/cost/secret、secret/env、Auth、DB/schema/RLS、Storage、OAuth、SMTP、真实数据、部署和 Production Gate 继续关闭。既有文档治理复审首轮 finding 已关闭；本次独立运行时复审首次结论为 REVIEW NO-GO，本批 findings 由 `c15b11c` 修复；独立定向复审针对 `c15b11c` 给出 REVIEW GO，无未关闭 P0/P1 finding；该 review GO 仅表示本批复审闭环，不改变 B1/B2 或完整 Gate；不得把历史 `ACTIVE_HEALTHY` 或 health 200 外推为当前持续健康或 Auth/DB 技术通过。

### 2026-08-28｜G2-A1 A1-B1 最小 Auth spike 风险 Gate 已形成，待独立安全复审（历史快照）

- 本批从远端 `main=3b2b8aed4c97b64f36338021adae0a45bec2215d` 建立隔离 docs-only worktree/分支，完成 G2-A1 关键风险批次的复用优先预检和最小 Auth spike 风险 Gate 设计；没有修改源码、依赖、lockfile、workflow、配置或外部资源。
- （历史快照）Gate 当时状态为**已形成、待独立安全复审**。G2-A1 技术阶段仍为“准备中”，完整 resource/cost/secret/Auth/DB Gate 仍关闭；当时不读取 project URL/key，不配置 Auth/OAuth/SMTP/Storage，不建表/写数据，不创建真实账号/fixture，不部署、不触碰 Production。
- Gate 目标范围限定为独立 Supabase Free 组织 `Rebuy Lab`、项目 `rebuy-auth-spike`、`eu-central-1`（Frankfurt）、EU non-production、synthetic-only。已核验 provider quote 为 `amount=0`、`recurrence=monthly`，API 未返回 currency；Owner 已确认实际 quote 并完成 `confirm_cost`；管理面为 `ACTIVE_HEALTHY`。这些事实不等于 Auth/MFA/session/DB/RLS/Storage/OAuth/SMTP/SSR 技术通过，也不等于税费、Spend Cap 或完整成本责任确认。
- （历史快照）若独立复审与 Owner 最终 Gate 通过，下一批 B1 只允许：复核官方 docs/changelog；只读取得 project URL + publishable key；确认 `.gitignore` 阻止 env；复用/定向扩展现有 SSR client 与 health route；配置 local synthetic-only 连接并验证不依赖 service_role；记录 Free limitations、STOP、expiry/cleanup 与回退。project URL/key 仅可进入 gitignored local env 或受控 Preview env；不得进入仓库、证据、聊天、日志、截图或客户端 bundle。
- 责任默认落账：Product/cost/stop/provider admin=Hexiang Huang；技术执行=Codex 自动化执行（后续 `luna_worker / max` 按批次执行，Owner 保留责任和停止权）；独立安全复审=独立只读 Codex reviewer + Owner 最终 Gate；secret owner=Hexiang Huang，原值只在 provider/Vercel secret store 或 gitignored local env；证据保管=仓库脱敏摘要 + provider 审计记录；法律/隐私/税务=A5/专业顾问待处理。
- 复用预检结论：复用 `prototype/lib/supabase/client.ts`、`server.ts`、`config.ts`、`.env.example` 与 `/api/health/supabase`；保留 `LoginPrototype.tsx` 的本地演示边界；暂不新建 Auth handlers/hooks/types/test suite；复用固定的 `@supabase/ssr=0.12.5`、`@supabase/supabase-js=2.112.4`、Node `22.x`、pnpm `10.33.3`。完整结论见[G2-A1 B1 风险 Gate 证据](./evidence/G2-A1/2026-08-28-b1-risk-gate/README.md)。
- 本批验证：仅运行文档/静态检查，不运行 build/lint/typecheck；因为源码、依赖、workflow、lockfile、配置和生成物均未变化，可复用当前 `main` Actions 的既有全绿证据。（历史快照）该时点独立安全复审尚未完成；B1 不自动打开 B2/B3、P2 或 Production。

### 2026-08-28｜G2-A1 B1 风险 Gate 独立复审收口与最小范围授权（历史授权快照）

- 独立安全复审首轮结论为 **NO-GO**；唯一的 P1 finding 是本批文档状态漂移，另有一项 P2 旧证据表述已按历史快照标注。实现代理按范围修复；主代理只读核对精确 diff 后关闭 finding。本记录不声称 reviewer 第二轮复审，也不补写未保留的具体分钟。
- Owner 当前消息批准继续；该批准仅打开 A1-B1 最小技术范围（已授权/待执行）：只读取得 project URL + modern publishable key，确认 `.gitignore` 阻止 env，仅存入 gitignored local env，复用/定向扩展现有 SSR/client/health 做 EU non-production synthetic-only 连接验证，并记录 Free 限制、STOP/cleanup。后续执行结果见下方当前条目。
- 这不是 broad waiver（全局豁免）。完整 resource/cost/secret/Auth/DB/Storage/OAuth/SMTP/真实 PII/部署/Production Gate 继续 CLOSED；service_role、secret key、db password 继续禁止；在该历史授权时点 G2-A1 技术阶段仍未开始，不得写成 G2-A1 Auth/B1 已通过，也不得自动打开 B2/B3、P2 或 Production。

### 2026-08-28｜G2-A1 A1-B1 最小本地连接验证完成（历史快照；当前配置/能力预检见下方）

- （历史快照）当前状态为：**G2-A1 执行中（仅 B1 最小连接验证已完成）**。初次 B1 运行前 connector 曾精确核对目标 Free/EU non-production 项目；独立运行时复审期间 connector 再次无法列出精确目标，当前管理面状态无法复验。未访问其他项目、未执行任何外部动作；此前本地 health `200` 仅为当时运行窗口的时间界定证据，不是当前持续健康保证。Chrome DOM 与内部交接曾出现一个截断的 publishable 参数展示值，该值触发 `401` 后停止，且不是当前 active key。`prototype/.env.local` 当前为 ignored 且 owner-only 权限类别；tracked tree/diff 未发现完整 active key、host、project ref 或 secret。
- 复用现有 prototype 配置、browser/SSR client 与 `/api/health/supabase`；无配置路径返回固定 HTTP `503`，此前真实 local synthetic-only 连接返回固定 HTTP `200`，均带 `Cache-Control: no-store`；该 200 仅为时间界定证据。localhost agent-browser 完成 networkidle、非空/无可见错误覆盖层、console errors 为空、关键元素快照及搜索→结果→商品详情导航。
- 本批未修改源码、依赖、lockfile、workflow 或配置；生成的 `next-env.d.ts` 漂移已按 HEAD 恢复。既有 Node22/pnpm 验证证据仍有效，本次文档差异不重复构建。
- 当前仅能判定 B1 最小连接验证完成，不能写成 G2-A1 Auth/B1 技术通过。既有文档治理复审首轮 finding 已关闭；本次独立运行时复审首次结论为 REVIEW NO-GO，本批 findings 由 `c15b11c` 修复；独立定向复审针对 `c15b11c` 给出 REVIEW GO，无未关闭 P0/P1 finding；该 review GO 仅表示本批复审闭环，不改变 B1/B2 或完整 Gate。Auth/OAuth/SMTP/DB/Storage/user/session/MFA、完整 resource/cost/secret、真实 PII、部署与 Production Gate 继续 CLOSED；service_role、secret key、db password 继续禁止。
- （历史快照）下一步仅为 B1 配置/能力只读预检，不自动进入 B2。脱敏结果见[B1 风险 Gate 证据](./evidence/G2-A1/2026-08-28-b1-risk-gate/README.md)，阶段合同见[G2-A1 准备与资源门禁](./stages/G2-A1-Auth-Spike准备与资源门禁.md)。

### 2026-08-28｜G2-A1 B1 配置/能力只读预检完成（当前状态）

- B1 配置/能力只读预检已完成：仅通过已登录 Supabase dashboard 的指定 Auth 配置页面读取 signup、provider、URL/redirect、邮件/SMTP、MFA、session、rate-limit、attack-protection 与 hooks 的脱敏分类；未点击 Save、Enable、Create、Reveal 或 Copy，未读取 API Keys、SQL/Table Editor、DB/Storage 数据、用户或 Audit Logs。
- 当前 G2-A1 仍为执行中；本批 findings 由 `7952d16` 修复；独立定向复审对该 exact head 给出 REVIEW GO，无未关闭 P0/P1/P2；该 GO 只关闭 B1 capability preflight 审查，不打开 B2 或任何 Auth/DB/Storage/OAuth/SMTP/费用/部署/Production Gate；不预写 push、PR、Actions 或 merge。本批不把配置能力观察写成 Auth 登录、OAuth callback、邮件、用户、session、MFA、DB、Storage 或 SSR 技术通过。B2 专项风险 Gate 草案已写入[B1 配置/能力预检证据](./evidence/G2-A1/2026-08-28-b1-capability-preflight/README.md)，但 B2 实施继续 CLOSED，须经独立安全复审和 Owner/主代理 Gate 后才可打开。
- 完整 resource/cost/secret/Auth/DB/Storage/OAuth/SMTP/真实 PII/部署/Production Gate 继续 CLOSED；service_role、secret key、db password、真实邮箱/账号/数据、生产 callback、付费升级和任何外部写入继续禁止。connector 精确目标当前不可复验这一残余风险保持记录。

## 6. 维护责任

- 任何阶段状态变更必须先更新本台账，再更新对应阶段记录和必要导航摘要。
- 阶段记录必须使用 [阶段执行记录模板](./templates/阶段执行记录模板.md)，并在 [阶段记录索引](./stages/README.md) 登记。
- 无 commit、deploy ref 或环境证据时统一写 `N/A`，不得凭空补全。
- 本台账不存放 secrets、PII 或原始附件；敏感证据只记录最小必要摘要、脱敏路径和 Owner Gate，不复制内容。
